# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- Initial project scaffold: npm workspaces monorepo (`backend`, `frontend`).
- `CLAUDE.md` governance/workflow rules and `docs/` structure (architecture, api, database, patterns, decisions, project-memory, troubleshooting, rsp-domain-spec).
- Backend: Express + TypeScript API with feature-first Clean Architecture (`modules/{auth,applicants,job-postings,applications}`), Prisma ORM against MySQL, JWT authentication, centralized error handling, request validation (Zod), Pino logging.
- Frontend: React + TypeScript (Vite) SPA with feature-first structure, JWT-aware API client.
- Applicant Registration feature (Application phase of the RSP pipeline): account registration/login, demographic profile, work experience, learning & development interventions, eligibility checklist with proof upload, awards, IPCR/Designation-to-Higher-Position upload for promotional applications, job posting listing, and application submission.
- Local MySQL database provisioned (via XAMPP's bundled MariaDB) and initial Prisma migration applied, creating all tables per `docs/database.md`. Seed data adds one admin user and two sample job postings.
- Applicant Registration wizard now auto-advances to the next step when the demographic profile is first created, and every step has Back/Next navigation (`frontend/src/features/applicant-registration/pages/RegistrationWizardPage.tsx`).
- Admin panel, separated from the applicant experience by role-gated routing (`/admin/*` vs `/jobs`, `/registration`, `/applications`): a "Post a Job" page and an "Evaluate Applicants" page (score 0-100, qualified/not-qualified decision, remarks per application). Backend: `evaluationScore`/`evaluationRemarks`/`evaluatedAt`/`evaluatedByUserId` added to `Application`, plus `GET /api/applications` (admin listing, optionally filtered by job posting) and `PATCH /api/applications/:id/evaluate` (both `ADMIN`-only). Frontend: new `admin` feature, and `ProtectedRoute` gained a `role` prop enforced on both the admin and applicant route groups.
- Fixed an auth race condition where an already-logged-in user landing on `/` was bounced to `/login` and stuck there (session hydration from `localStorage` happened after route guards had already decided). `AuthContext` now exposes `isLoading`; `ProtectedRoute`/`HomeRedirect` wait on it.
- Admin panel redesigned around a sidebar shell (`AdminShell`) with four sections, and full CRUD added for Job Management and (new) Users Management, plus a new read-only History of Logs section:
  - **Job Management**: `JobManagementPage` replaces the old create-only "Post a Job" page with a data table (Edit/Delete per row, inline delete confirmation, no native browser dialogs). Backend: `PATCH`/`DELETE /api/job-postings/:id` (`ADMIN`-only); deleting a posting with existing applications is blocked (409) to prevent silent data loss via cascade.
  - **Users Management** (new): `UsersManagementPage` - admin-provisioned account creation (including new admins), role/email editing, deletion with a self-delete guard. Backend: new `users` module (`GET/POST /api/users`, `PATCH/DELETE /api/users/:id`, all `ADMIN`-only).
  - **History of Logs** (new): `AuditLogsPage`, read-only, filterable by entity type. Backend: new `audit_logs` table and `audit-logs` module (`GET /api/audit-logs`, `ADMIN`-only, no write endpoints by design). Wired into Users/Job Postings/Application-evaluation services to record `USER_*`, `JOB_POSTING_*`, and `APPLICATION_EVALUATED` entries.
  - Schema: `JobPosting.createdByUserId` and the new `AuditLog.actorUserId` are nullable with `onDelete: SetNull`, so deleting a user never blocks on, or cascades into deleting, their past job postings/log entries.
- Unified applicant registration into a single continuous flow instead of "sign up, then fill in your info after logging in": `RegisterPage` and `RegistrationWizardPage` were merged into one `RegistrationPage` (`/register`) that carries an unauthenticated visitor through account creation and straight into demographic profile, work experience, L&D, awards, and documents with no route change in between. Backend: added `Applicant.registrationCompletedAt` and `POST /api/applicants/me/complete-registration` (validates an eligibility-proof upload exists if `hasEligibility` is true). Frontend: `AuthContext` now resolves `registrationComplete` for the session; `ProtectedRoute` and `HomeRedirect` redirect any applicant who hasn't finished registration to `/register` regardless of which page they try to reach directly, so browsing job postings or applying is unreachable until every step is done. See `docs/decisions.md` for the full rationale.
- `JobManagementPage` and `UsersManagementPage` switched from an always-visible inline create/edit form above the table to a table-only layout with an "Add Job"/"Add User" button that opens the same form in a `Modal` (also reused for editing an existing row). `EvaluateApplicantsPage`'s applicants table now always renders once a job posting is selected, showing an in-table "No applications for this posting yet." row instead of hiding the table when the list is empty - consistent with `AuditLogsPage`'s existing empty-state pattern. Added shared `.page-header` (title + action button row) and `.table-empty` (centered empty-state cell) styles in `frontend/src/shared/styles/index.css`.
- Admin **Dashboard** (new, `/admin/dashboard` - now the admin role's landing page in place of `/admin/jobs`): stat tiles for applicants/job postings/applications/users, a horizontal bar chart of applications by status (reusing the same status colors as the app's `.badge`s) and top job postings by application count, plus a recent-activity feed reusing `AuditLogsPage`'s row shape. Backend: new `dashboard` module, `GET /api/dashboard/summary` (`ADMIN`-only) aggregating counts directly off `User`/`Applicant`/`JobPosting`/`Application` via Prisma `groupBy`, plus the 8 most recent audit log entries. No new tables. Extracted `AuditLogsPage`'s action-formatting helper into `shared/utils/formatAuditAction.ts` so both pages use it.
- Dashboard branding pass: a navy-to-navy-hover gradient hero banner with a gold bottom border (`.dashboard-hero`, reusing the same gradient recipe as the header's brand mark), branded icon chips per stat tile (navy gradient tile, gold glyph - inline SVG, no icon library), a gold top border on the stat tiles and chart cards, and stat-tile values recolored to the brand navy. The top-job-postings bars switched from navy to gold (`--color-accent-hover`) so they read distinctly from the status-colored applications-by-status bars next to them.
- `JobManagementPage` gained client-side search-by-title and a position-level filter, shown above the table only once postings exist. Distinguishes "no postings yet" from "no postings match your search/filter" in the table's empty state. New shared `.filters-row` style for this search+filter toolbar layout.
- **Interview panel accounts & CompAss tabulation** — replaces the single-admin-score evaluation stand-in with the real multi-evaluator Evaluation phase from the RSP spec:
  - New `PANEL` role (provisioned the same way as `ADMIN`, via Users Management's existing role dropdown) and `Application` status `FOR_INTERVIEW`.
  - **Evaluation Criteria** (new admin page, `/admin/criteria`): admin-editable interview rubric (`EvaluationCriterion`: name + max score, deactivate instead of delete once scored). Backend: new `evaluation-criteria` module, `GET /api/evaluation-criteria` (`ADMIN`/`PANEL`), `POST`/`PATCH`/`DELETE` (`ADMIN`-only, 409 if the criterion has recorded scores).
  - **Interview Panel** (new admin page, `/admin/panel-interviews`): assign/unassign `PANEL` users to a job posting's interview board, per posting. Backend: new `panel-assignments` module (`ADMIN`-only CRUD on `PanelAssignment`, unique per posting+panelist).
  - **My Interviews** (new panel-only page, `/panel/interviews`, new `panel` frontend feature): a panelist's queue of `FOR_INTERVIEW` applications across their assigned postings, each scored against every active criterion + remarks. Backend: new `panel-evaluations` module — `GET /my-queue` and `PATCH /:applicationId` (both `PANEL`-only; upserts `PanelEvaluation` + child `PanelScore` rows in one transaction, validates every active criterion is scored and no score exceeds its max).
  - `EvaluateApplicantsPage` extended: a "Schedule Interview" action moves an eligible application to `FOR_INTERVIEW` (new `PATCH /api/applications/:id/schedule-interview`, `ADMIN`-only); two new columns, "Panel Avg" and "Rank", show the live CompAss matrix (`GET /api/panel-evaluations/tabulation/:jobPostingId` — sums/averages/ranks panelists' scores per application); the expanded evaluate form shows each panelist's score and warns if not everyone assigned has submitted yet, since finalizing early is still allowed.
  - Frontend role/routing: `ProtectedRoute`'s `role` prop now accepts an array (`AppRole | AppRole[]`), `Layout` gained a `PANEL` nav branch, `HomeRedirect`/`HOME_BY_ROLE` route `PANEL` to `/panel/interviews`.
  - Fixed two related gaps while adding the role: `backend/src/modules/auth/auth.service.ts`/`auth.dto.ts` had a hardcoded `"APPLICANT" | "ADMIN"` literal union instead of Prisma's `Role` type (would have broken panel login); the `dashboard` module's role/status tallies didn't know about `PANEL`/`FOR_INTERVIEW` and would have silently undercounted them.
  - Schema: four new tables (`evaluation_criteria`, `panel_assignments`, `panel_evaluations`, `panel_scores`) in one migration alongside the two enum additions. See `docs/decisions.md` for the scope calls (multi-criteria rubric over one overall score; admin-assigned panelists over global visibility; `evaluate` left without a completeness guard).
- Applicant Registration: validation is now specific to the exact field that's missing, shown inline under that field, and (where it blocks progress) checked client-side before the network round trip:
  - Account step (email/password) and Demographic Profile (all required fields) now validate client-side with plain messages ("First name is required.", "Contact number is required.", etc.) instead of relying solely on a generic error banner plus whatever wording the backend's Zod validator happened to produce.
  - The Work Experience / Learning & Development / Awards "Add entry" mini-forms gained the same per-field required checks (these steps themselves stay optional — an applicant can legitimately have none of one — only the individual "Add" action is validated when used).
  - The Documents step now disables "Finish registration" with an inline explanation ("You indicated you have a civil service eligibility — upload proof of it above...") when eligibility proof is required but missing, instead of only surfacing that as a generic banner after a failed submit attempt.
- Job postings gained a `description` field (free-text role/duties overview, distinct from the existing `qualification*` QS fields): required in the admin Add/Edit Job modal (`JobManagementPage`), and shown to applicants only inside a new "View Details" modal on `JobPostingsListPage` rather than on the list itself. The applicant-facing card is now a minimal summary (title, status, level, closing date) plus "View Details" and "Apply" buttons; the modal carries the description, all four qualification standards, and the promotional-documents notice. Backend: `description` added to `JobPosting` (migration backfills the 3 seeded postings with a placeholder, since MySQL `TEXT` columns can't carry a schema-level default), `createJobPostingSchema`/`updateJobPostingSchema` updated accordingly.
- **Application stage tracker + stage-change emails**:
  - `MyApplicationsPage` now renders a 4-node stepper (Submitted → Sifting → Interview → Decision) per application (new `ApplicationStageTracker` component), replacing the raw status badge as the primary at-a-glance view - the badge stays alongside it as the precise label. Colors reuse the app's existing status language (the same tokens already used for `.badge`s and the Dashboard's bars) rather than a new palette. `WITHDRAWN` bypasses the linear stepper for a distinct muted state, since it can happen from any prior stage.
  - New email infrastructure (none existed before): `nodemailer` added to the backend; `EmailService` (`backend/src/shared/email/emailService.ts`) sends via real SMTP when `SMTP_HOST` is configured, otherwise logs the full message to the console prefixed `[DEV EMAIL]` - so the feature works and is verifiable in local dev with no mail server. A send failure is always caught and logged, never thrown; a notification is a side effect of a status change, not a precondition for it.
  - `ApplicationsService` emails the applicant after each real status-changing action: `submit()` (application received), `scheduleInterview()` (scheduled for interview), `evaluate()` (qualified/not qualified). Copy lives in `backend/src/shared/email/applicationEmailTemplates.ts`, separate from the service logic. No hook exists for `UNDER_SIFTING`/`WITHDRAWN` - nothing in the codebase sets either status yet.
  - New optional `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASSWORD`/`SMTP_SECURE`/`SMTP_FROM` env vars (see `.env.example`).
- Job postings gained `monthlySalary`, `placeOfAssignment`, and `duties` fields (all free-text, required), bringing them up to the level of detail on typical DILG job-posting flyers: `monthlySalary` shows on both the admin table and the applicant-facing list card (a new "Salary" column / meta row); `placeOfAssignment` and `duties` show only in the admin Add/Edit Job modal and the applicant "View Details" modal, alongside `description` and the qualification standards. `duties` is entered as one duty per line and rendered as a numbered list (split on `\n`). Backend: migration `20260808044639_add_salary_place_duties_to_job_posting` follows the same add-nullable → backfill → enforce-NOT-NULL pattern used for `description`, since MySQL `TEXT`/`VARCHAR` columns can't carry a schema-level default; the 3 seeded postings were backfilled with realistic values. `createJobPostingSchema`/`updateJobPostingSchema` updated accordingly.
- **Sifting / PQE / Interview pipeline** — fills in the previously-nonfunctional `UNDER_SIFTING` status and repurposes the old free-standing "Evaluate Applicants" action into the actual RSP pipeline order (Submitted → Sifting → Qualified → PQE score → Interview → panel tabulation):
  - Applications now move to `UNDER_SIFTING` automatically the instant they're submitted (`ApplicationsRepository.create()` sets it directly) - no manual "start sifting" step.
  - `PATCH /applications/:id/evaluate` (any status → `QUALIFIED`/`NOT_QUALIFIED`, with a 0-100 score) is now `PATCH /applications/:id/sift` (`UNDER_SIFTING` only → `QUALIFIED`/`NOT_QUALIFIED` + optional remarks, no score - sifting is pass/fail against the posting's qualification standards, not a scored evaluation). `EvaluationRow`'s form and button label updated to match ("Sift"/"Sift Applicant" instead of "Evaluate"/"Re-evaluate"); re-sifting after a decision is no longer offered.
  - New PQE score import: `POST /applications/import-exam-scores` (admin, multipart `.xlsx`/`.xls` + `jobPostingId`) parses an Excel file (new `exceljs` dependency) with "Name"/"Score" header columns, matches each row by normalized full name against that posting's `QUALIFIED` applicants, and records `Application.examinationScore`/`examinationScoredAt` on matches - unmatched rows come back in the response (not an error) for the admin to review and re-upload. `EvaluateApplicantsPage` gained an "Import PQE Scores" file upload control with an inline unmatched-rows list, and an "Exam Score" table column.
  - `PATCH /applications/:id/schedule-interview`'s guard changed from "status is `SUBMITTED` or `UNDER_SIFTING`" to "status is `QUALIFIED` **and** `examinationScore` is recorded" - interview scheduling now enforces sifting-then-PQE order. `EvaluationRow` shows an "Awaiting PQE score" hint on Qualified applications without a score yet, in place of the (disabled) button.
  - Schema: `Application.evaluationScore` dropped; `evaluationRemarks`/`evaluatedAt`/`evaluatedByUserId` renamed `siftingRemarks`/`siftedAt`/`siftedByUserId`; added `examinationScore`/`examinationScoredAt`. New audit actions `APPLICATION_SIFTED`/`APPLICATION_EXAM_SCORES_IMPORTED` replace `APPLICATION_EVALUATED`.
  - `ApplicationStageTracker`'s 4-node stepper is now Submitted → Sifting → Examination → Interview (previously Submitted → Sifting → Interview → Decision) - the Qualified/Not-Qualified resolve-in-place styling moved from the old terminal "Decision" node onto the Sifting node itself, since sifting is now mid-pipeline rather than a final call; a recorded PQE score shows as a line of text under the tracker on `MyApplicationsPage`.
  - Email copy updated: the Qualified sifting email now invites the applicant to the PQE instead of vaguely promising "next steps"; new `examScoreEmail` notifies the applicant of their recorded PQE score (sent regardless of score, per the domain spec).
  - See `docs/decisions.md`'s 2026-08-08 "Sifting/PQE/Interview pipeline" entry for the full rationale and trade-offs (notably: name-only PQE matching, no enforced pass/fail threshold on the exam score, and the pipeline still stopping at `FOR_INTERVIEW`/tabulation since Deliberation remains future work).
- Learning & Development entries can now have proof file(s) attached directly on `LdInterventionSection` (PDF/JPEG/PNG, ≤5MB, reusing the existing upload infrastructure) - each intervention shows its attached proof(s) with a per-file "Remove", plus a file input to attach another. Backend: new `DocumentType.LD_PROOF` and `Document.ldInterventionId` (nullable FK to `LdIntervention`, `onDelete: Cascade`); `POST /api/applicants/me/documents` accepts an optional `ldInterventionId` field (validated as belonging to the caller, same pattern as the existing `applicationId` field) alongside the existing `type`/`file`. The generic `DocumentsSection` (eligibility/IPCR/designation) no longer offers `LD_PROOF` in its type dropdown, since that upload is per-claim and lives in the L&D section instead.
- **Fix:** PQE score import (`POST /applications/import-exam-scores`) failed to match applicants who have a middle name on file, because the lookup only ever built a `firstName lastName` key while a real exam sheet naturally includes the middle name/initial (e.g. "Gibo R. Ormeneta" vs. the applicant's own `firstName`/`lastName` "Gibo"/"Ormeneta") - every row for such an applicant came back "unmatched" even though the name was, to a human, obviously the same. `normalizeName()` now also strips `.`/`,`, and a new `buildNameVariants()` indexes each Qualified applicant under every plausible spelling (with/without middle name, middle initial, suffix) instead of one fixed key.
- **Interview scheduling now captures date/venue/attire/notes, and the notification email actually contains them.** Previously "Schedule Interview" was a single button ("the panel will be in touch with further details") and no interview date was ever recorded anywhere. `PATCH /applications/:id/schedule-interview` now takes a `ScheduleInterviewDto` (`scheduledAt`, `venue`, optional `attire`/`notes`; 400 if `scheduledAt` isn't in the future), and `EvaluationRow`'s "Schedule Interview" action opens a form for these instead of firing immediately. Schema: `Application` gained `interviewScheduledAt`/`interviewVenue`/`interviewAttire`/`interviewNotes`. `forInterviewEmail` renders a When/Where/What-to-wear/Additional-instructions list instead of the old vague one-liner; the same details are shown to the applicant under their stage tracker on `MyApplicationsPage` once scheduled, and to the admin in `EvaluationRow`'s expanded panel.
- **Fix:** `SMTP_SECURE` was parsed with `z.coerce.boolean()` in `backend/src/config/env.ts`, which is just `Boolean(value)` under the hood - since any non-empty string is truthy in JS, `SMTP_SECURE=false` in `.env` was silently coerced to `true`, causing real SMTP delivery to fail with a TLS handshake error ("wrong version number") the moment real credentials were configured. Changed to `z.enum(["true", "false"]).transform(v => v === "true")`, which parses the string correctly. Real Gmail SMTP delivery (App Password) is now configured and verified working in this environment's `backend/.env` (gitignored, not committed).
- **Login page and the registration flow's account-creation step got a branded visual treatment**: a full-bleed navy-gradient panel (new `.auth-page` class, breaks out of `.app-main`'s centered column via a `left: 50%; margin-left: -50vw;` CSS trick) framed with gold top/bottom borders, the real DILG seal (`frontend/public/dilg-logo.webp`, provided by the user - not fabricated) shown crisply atop the login/account card and again, tiled and blurred, across the entire panel as background texture. `html`/`body` gained `overflow-x: hidden` to absorb the few px of horizontal overflow that breakout technique introduces (`100vw` counts the scrollbar's own width) - verified `.table-wrap`'s independent `overflow-x: auto` scroll regions are unaffected. Only these two screens changed; the rest of the registration wizard (once authenticated) keeps its normal centered-column layout.
  - Follow-up polish: the top nav is now hidden on both screens (`Layout.tsx`'s new `hideHeader`) for a fully immersive full-page look, with a small in-card "Don't have an account? Register" / "Already have an account? Log in" cross-link added to each so navigating between them still works without it. **Fix:** the login card rendered visibly narrower than the registration card despite sharing the same `.auth-form` CSS rule - both are flex items of `.auth-page`, and a flex item with only `max-width` set (no `width`) shrinks to fit its own content rather than filling up to that max, so the page with shorter copy (login) ended up narrower. Added `width: 100%` alongside `max-width: 400px` to fix it.
  - Second follow-up: removed the grey gap `.app-main`'s own padding left above/below the panel now that the header (and its vertical space) is gone - `.app-main:has(.auth-page)` zeroes that padding specifically on these two pages, and `.auth-page` uses `min-height: 100vh` to fill exactly the viewport (verified via `scrollHeight === clientHeight`, no scroll). Background watermark reverted from a full `background-repeat: repeat` tile (read as busy) back to two large blurred copies in opposite corners (720px, blur 24px) - bigger and softer than the original pass.
- **Fix: applicants without the eligibility a job posting requires could apply anyway.** `ApplicationsService.submit()` previously only checked that a self-declared-eligible applicant had uploaded *some* proof document, never that their eligibility type actually matched what the posting needed - and an applicant with no eligibility on file wasn't blocked at all. Job postings can now specify `requiredEligibilityTypes` (checkboxes on the admin Job Management form: RA 1080 / CSC Professional / CSC Sub-Professional / Barangay Eligibility, alongside the existing free-text qualification field, which stays as applicant-facing context only); submitting an application 400s unless the applicant's own `eligibilityType` is one of them, and `JobPostingsListPage` disables/relabels the "Apply" button using the same check so the applicant learns why before clicking, not after. A posting with none checked requires no eligibility, matching prior behavior. Schema: new `JobPostingRequiredEligibility` join table (MySQL has no native scalar-list column) - see `docs/decisions.md`'s 2026-08-08 "Eligibility gate at application submission" entry.
- **Applicants can now withdraw their own application.** `WITHDRAWN` has existed in the `ApplicationStatus` enum since the pipeline's first migration but nothing ever set it. New `PATCH /api/applications/:id/withdraw` (applicant-only, no admin equivalent): 404s if the application isn't the caller's own (can't distinguish "not found" from "not yours"), 400s if the application is already `NOT_QUALIFIED`/`WITHDRAWN` (both terminal) - otherwise allowed from `SUBMITTED`/`UNDER_SIFTING`/`QUALIFIED`/`FOR_INTERVIEW`. Sets `status: WITHDRAWN`, stamps new `withdrawnAt`, and sends a new `withdrawnEmail` notification (same pattern as every other stage-change email). `MyApplicationsPage` gained a "Withdraw" button (only shown while withdrawable) behind a `ConfirmDialog`, reusing the same confirm-before-destructive-action pattern as `JobManagementPage`'s delete. Consistent with `submit()`, this applicant-initiated action is not written to the audit log (that trail has only ever covered admin/panel writes) - see `docs/decisions.md`'s 2026-08-08 "Applicant-initiated application withdrawal" entry for the full rationale, including why there's no spec-mandated cutoff for which statuses allow it.
- **Self-service password change, for any logged-in role.** New `PATCH /api/auth/me/password` (`{ currentPassword, newPassword }`, `authenticate`-only - no role restriction - since it always acts on the caller's own account): 400 with a `currentPassword` field error if it doesn't match, otherwise re-hashes and saves `newPassword` (same `bcryptjs`/salt-rounds convention as registration) and returns 204. Neither `ADMIN` nor `PANEL` had any self-account page before this, and the applicant's "My Profile" link only ever reached demographic-profile fields, not account credentials - one new `ChangePasswordPage` (`/account/password`, `ProtectedRoute` with no `role` restriction) and one "Change Password" link in `Layout`'s header (shown for any authenticated role) now serve all three roles instead of three separate implementations. Does not invalidate other already-issued tokens for the same user - see `docs/decisions.md`'s 2026-08-09 entry for why, and for the still-missing "forgot password" (no current password known) case.
- **Fix: Dashboard stat tiles (e.g. "Users") didn't reflect a delete made while the Dashboard was already open.** `GET /api/dashboard/summary` was always correct and live (`prisma.applicant.count()`/`user.groupBy()` on every call, no caching anywhere) and deleting a user is a real cascading DB delete (`onDelete: Cascade` from `User` down through `Applicant` and everything under it) - the counts were only ever stale on the *frontend*, because `DashboardPage` fetched its summary exactly once on mount with no refetch trigger. Normal navigation away and back already remounted and refetched correctly; the bug only showed up on an already-open Dashboard tab (e.g. deleting from a second tab, or a browser back/forward-cache restore). Added a "Refresh" button in the dashboard hero (`frontend/src/features/admin/pages/DashboardPage.tsx`) plus automatic silent refetch on `window` focus / `visibilitychange`, so an open Dashboard now stays in sync without a manual page reload. The refetch is "silent" (only a small button-level spinner, not the full-page `LoadingBlock`) so existing numbers stay visible while refreshing instead of flashing to a loading skeleton.
- **Evaluate Applicants now shows every application across every job posting in one table, with a search box and a Job title filter**, replacing the old design where a required "Job posting" `<select>` gated the entire table to one posting at a time. `GET /api/applications` is now called with no `jobPostingId` (already-supported unfiltered mode) to load everything once; the CompAss tabulation (Panel Avg/Rank) is fetched per distinct job posting present in the results and kept in a `Record<jobPostingId, TabulationResult>` map, so those columns stay correct per-posting even with rows from several postings visible together. The search box filters client-side by applicant name/email; the new "Job title" `<select>` (default "All job postings") filters by posting and doubles as the target-posting picker for the PQE Excel import, which still only makes sense for one posting at a time - the import panel is hidden behind a hint ("Select a specific job title above...") until a specific title is chosen. New "Job Posting" column added to `EvaluationRow` so a row's posting is visible even when the table holds several. Reused `JobManagementPage`'s existing `.filters-row` search+filter layout and "no results match your search/filter" vs. "no data at all" empty-state distinction rather than inventing a new pattern.
- **Eligibility type labels now use official CSC terminology**: "CSC Professional" → "Second-Level Eligibility (Professional)", "CSC Sub-Professional" → "First-Level Eligibility (Subprofessional)" (RA 1080 and Barangay Eligibility unchanged). Changed in the single shared `ELIGIBILITY_OPTIONS`/`ELIGIBILITY_LABELS` source (`frontend/src/shared/constants/eligibility.ts`), so the new wording shows consistently everywhere it's used - the registration Demographic Profile eligibility dropdown, the admin Job Management "Required eligibility" checkboxes, and the applicant-facing "Requires eligibility: ..." text on job postings - rather than only on the screen that prompted the change. The backend's matching (necessarily duplicated, no shared package between workspaces) label map in `applications.service.ts`, used in the eligibility-gate rejection message, was updated to match so the wording is consistent between what an applicant sees on the form and what they see in an error message. Underlying `EligibilityType` enum values (`CSC_PROFESSIONAL`/`CSC_SUBPROFESSIONAL`) are unchanged - display text only.
- **RA 1080 and Barangay Eligibility removed from the selectable eligibility choices** (client request) - only Second-Level (Professional) and First-Level (Subprofessional) remain pickable in the registration Demographic Profile form and the admin's "Required eligibility" checkboxes. Non-destructive: `ELIGIBILITY_OPTIONS` (drives the selectable UI) shrank to 2 entries, but `ELIGIBILITY_LABELS` (drives display) still has all 4 plus `NONE`, so any pre-existing data with `RA1080`/`BARANGAY` already set (an applicant's `eligibilityType`, or a job posting's `requiredEligibilityTypes`) keeps rendering a proper label instead of blank/undefined - no schema change, no migration, no enum values removed on the backend. No new "select a value that's about to disappear from the list" edge case in practice today since no current data relied on `BARANGAY`.
- **Documents step now lists the full official application-documents checklist**, and Designation to a Higher Position (and Performance Rating) are no longer required to apply, even to a promotional posting:
  - `DocumentsSection` renders all 10 official checklist items (Application Letter through Proof of Award(s)) with Required/Optional badges, including the Application Letter's exact addressee/thru block (Regional Director, thru the Human Resource Section head) as static instructional text - not just a bare type-picker dropdown as before.
  - Schema: `DocumentType` expanded from 5 to 11 values - new `APPLICATION_LETTER`, `PDS`, `TRANSCRIPT_OF_RECORDS`, `DIPLOMA`, `PQE_NOTICE`, `AWARD_PROOF` (migration `20260809084501_expand_document_types`); `IPCR`/`DESIGNATION_ORDER` reused as-is with new labels ("Performance Rating (Last Rating Period)", "Proof of Designation") rather than adding redundant types.
  - `ApplicantsService.completeRegistration()` now also requires `APPLICATION_LETTER` and `PDS` documents (400 without them), alongside the existing `ELIGIBILITY_PROOF`-if-`hasEligibility` check. Every other checklist item stays genuinely optional, matching the checklist's own "if applicable"/"if any" wording.
  - **Fix/change:** `ApplicationsService.submit()`'s `PROMOTIONAL`-posting hard block (previously required an uploaded `IPCR` and `DESIGNATION_ORDER` or the application was rejected with 400) was removed entirely - both documents are now general optional registration documents like the rest of the checklist, not a per-application gate. `JobPostingsListPage`'s matching "Promotional applications require an uploaded IPCR and Designation..." notice was removed since it's no longer true.
  - The "Document type" dropdown's option labels no longer carry a literal `"(promotional)"` suffix (was on both `IPCR` and `DESIGNATION_ORDER`).
  - See `docs/decisions.md`'s 2026-08-09 "Full official documents checklist" entry for the full rationale.
- **Fix: Demographic Profile falsely rejected a selected eligibility type with "Choose which eligibility you hold."** `DemographicProfileForm`'s `eligibilityType` state defaulted to `"NONE"`, but the `<select>` never renders a `"NONE"` `<option>` (only real eligibility types) - when a controlled `<select>`'s value doesn't match any of its options, the browser visually displays the *first* option as selected while React's actual state is unaffected until the user picks a genuinely different option. So checking "I have a civil service eligibility" made the dropdown visibly show a type (e.g. "Second-Level Eligibility (Professional)") as already selected, while the form's real state remained `"NONE"` - submitting then failed client-side validation with a confusing "already selected but rejected" experience. This bug has existed since the form was first built; it only became reliably visible once RA 1080 was removed from the option list, since CSC Professional then became the type occupying that default-looking first slot. Fixed by defaulting `eligibilityType` to the first real option the moment the checkbox is checked (only when it's still `"NONE"`, so toggling the checkbox off and back on during a profile edit doesn't clobber an existing selection), so the visible selection and React's state can no longer disagree. Verified via a full repro: checked the box without ever touching the dropdown, submitted, confirmed success and that the saved `eligibilityType` matches what was visually shown.
- **Learning & Development and Award entries now require proof of the specific claim, extending the exact rule Eligibility already used** ("claimed something → prove that specific thing"), per client request:
  - `ApplicantsService.completeRegistration()` now requires a matching `LD_PROOF`/`AWARD_PROOF` document for **every** `LdIntervention`/`Award` entry the applicant added - not just one proof file existing somewhere - and 400s naming the specific entry's title if one's missing, so it's clear exactly which claim still needs proof.
  - **Awards can now have proof file(s) attached per entry**, matching how Learning & Development already worked: `AwardsSection` rebuilt to mirror `LdInterventionSection`'s per-row upload/list/remove UI exactly (shared `.proof-item` style, renamed from `.ld-proof-item` since it's no longer L&D-only). Schema: new `Document.awardId` (nullable FK to `Award`, `onDelete: Cascade`, migration `20260809093325_add_document_award_id`) mirrors the existing `ldInterventionId` pattern; `POST /api/applicants/me/documents` accepts an optional `awardId` field (validated as belonging to the caller, same as `ldInterventionId`).
  - `AWARD_PROOF` removed from the generic Documents-step type dropdown (mirroring `LD_PROOF`, already excluded) since it's now uploaded per-entry from the Awards section instead - a generic untied upload there wouldn't have satisfied the new per-entry requirement anyway.
  - `RegistrationPage`'s "Finish registration" gate and missing-documents warning list extended to name every L&D/award entry still missing proof.
  - See `docs/decisions.md`'s 2026-08-09 "L&D and Award proof required per entry" entry for the full rationale.
- **Fix: re-uploading Application Letter/PDS/etc. created duplicates instead of replacing the existing file.** `DocumentsService.upload()` now deletes any existing document (DB row + file on disk) of the same type for the applicant first, for the 8 document types that represent a single current document (`APPLICATION_LETTER`, `PDS`, `IPCR`, `ELIGIBILITY_PROOF`, `TRANSCRIPT_OF_RECORDS`, `DIPLOMA`, `PQE_NOTICE`, `DESIGNATION_ORDER`) - re-uploading replaces rather than accumulates. `LD_PROOF`/`AWARD_PROOF` (multiple proofs per entry is intentional) and `OTHER` (miscellaneous catch-all) are unaffected. `DocumentsSection` mirrors the same type set client-side to drop the now-stale local entry immediately and show "replaced" instead of "uploaded" in the toast, and its intro copy now says re-uploading replaces the existing file. Verified: uploaded Application Letter twice, confirmed only one row remains (different id, old row and old file both gone), and confirmed L&D proof still correctly allows multiple files per entry. See `docs/decisions.md`'s 2026-08-09 "Re-uploading a single-instance document type replaces it" entry for the replace-vs-reject-vs-confirm tradeoff.
  - **Follow-up (same day), per client request:** the "Document type" dropdown in `DocumentsSection` now removes a single-instance type from the list entirely once it's already on file, instead of leaving it selectable and relying on the silent replace above - the type reappears only after the applicant removes the existing one via the table's "Remove" button. Awards were explicitly confirmed out of scope here and left untouched: each award already has its own independent per-row upload slot in `AwardsSection` (N awards → N upload contexts, each accepting multiple proof files), unrelated to this generic dropdown. Fixed the same "controlled `<select>` value must match a rendered `<option>`" class of bug this session's earlier eligibility-dropdown fix addressed: the type dropdown's initial state is now computed lazily from what's actually available, and auto-advances to the next available type immediately after an upload removes the just-uploaded type from the list. Verified in-browser: uploading Application Letter removes it from the dropdown and auto-selects PDS next; removing that Application Letter document brings it back into the list.
  - **Second follow-up (same day), per client request: L&D and Award proof reversed from "multiple proofs per entry" to one proof per entry.** `DocumentsService.upload()`'s replace-on-reupload rule (above) now also covers `LD_PROOF`/`AWARD_PROOF`, scoped per-entry instead of per-applicant - uploading a new proof for a given `LdIntervention`/`Award` row deletes that row's existing proof (DB row + file on disk) first, rather than accumulating. `LdInterventionSection`/`AwardsSection` hide the file `<input>` for a given entry once it has a proof, showing it again only once that proof is removed via the existing per-row "Remove" button - so an entry can have at most one proof file at a time in the UI as well as the database. `OTHER` remains the sole exempt, genuinely-multi-file type. Verified in-browser end to end: an entry with no proof shows the file input; uploading makes the input disappear, leaving only the filename + Remove; clicking Remove (through the existing confirm dialog) brings the input back.
- **PDS now requires both a PDF copy and an Excel copy, and every document type enforces its own file type instead of one blanket PDF/JPEG/PNG rule for everything.** Per client request ("Applicants are required to upload Pdf and excel file of PDS. Make sure system will check the file type correctly."): new `DocumentType.PDS_EXCEL` (migration `20260809105041_add_pds_excel_document_type`) is a second required single-instance slot alongside the existing `PDS` (now labeled "— PDF copy" / "— Excel (CS Form 212) copy" in the UI); `ApplicantsService.completeRegistration()` 400s unless both are on file. File-type checking is now genuinely per-type: `documents.upload.ts`'s multer `fileFilter` was widened to accept the union of every type's allowed MIME types (PDF/JPEG/PNG plus the two Excel MIME types already used by the PQE score import) as an outer gate, and a new authoritative check - `assertMimeTypeAllowed()` in `documents.service.ts`, keyed by `ALLOWED_MIME_TYPES_BY_DOCUMENT_TYPE` - runs after multer has fully parsed the request and 400s if the uploaded file doesn't match what that specific `type` expects (every type defaults to PDF/JPEG/PNG except `PDS_EXCEL`, which requires XLSX/XLS). This closes a real gap, not just a PDS-specific one: previously nothing stopped an Excel file (or any allowed MIME type) from being uploaded as any document type. `DocumentsSection`'s file input `accept` attribute and a new client-side `validateFileType()` check both switch based on the selected type; changing the "Document type" dropdown now clears any already-picked file so a selection can't silently carry over to a type it isn't valid for. Verified via direct API calls: PDS_EXCEL-as-JPEG rejected, PDS_EXCEL-as-XLSX accepted, PDS-as-XLSX rejected, PDS-as-PDF accepted, APPLICATION_LETTER-as-XLSX rejected, and `complete-registration` correctly blocks/allows based on whether both PDS documents are present. See `docs/decisions.md`'s 2026-08-09 "PDS split into a required PDF copy and a required Excel copy" entry, including a called-out pre-existing (not new) orphaned-file-on-rejected-upload gap.
- **Application Letter moved from a one-time registration upload to a per-application upload, attached when actually applying to a job posting.** Per client request: the letter is addressed to a specific vacancy, so requiring one generic copy at registration never matched how the document is really used. `completeRegistration()` no longer checks for it, and it was removed from the generic `/applicants/me/documents` endpoint's `type` enum entirely (nothing legitimate to tie it to there). `POST /api/applications` changed from a plain JSON `{ jobPostingId }` body to `multipart/form-data` (`jobPostingId` + `file`) - the Application Letter (PDF/JPEG/PNG, ≤5MB) is now required to apply (400 without one), and `ApplicationsRepository.createWithApplicationLetter()` creates the `Application` and its `Document(type=APPLICATION_LETTER, applicationId=...)` together in one DB transaction, so the two can never exist independently. `JobPostingsListPage`'s "Apply" button now opens a modal (file input, plus the DILG addressee/thru block moved over from the old registration Documents-step checklist row) instead of submitting on a single click - unavoidable now that applying carries a file. A small `createDiskStorage()` helper was factored out of `documents.upload.ts` into `backend/src/shared/upload/diskStorage.ts` so the new `applicationLetter.upload.ts` multer config doesn't duplicate the disk-storage setup a third time. Verified via direct API calls: registration completes without an Application Letter; applying without a file 400s; applying with a non-PDF/JPEG/PNG file 400s; applying with a valid PDF succeeds and the resulting `Document` is correctly tied to the new `applicationId`; uploading `type=APPLICATION_LETTER` via the generic documents endpoint now 400s as an invalid enum value; duplicate-application and eligibility-gate checks still behave exactly as before. See `docs/decisions.md`'s 2026-08-09 "Application Letter moved from a registration-time requirement to an apply-time requirement" entry.
  - **Follow-up (same day), per client request:** the "Apply" button now shows as a disabled "Already Applied" once the applicant has submitted to that posting, instead of staying clickable (and relying on the backend's 409 if clicked again). `JobPostingsListPage` fetches `listMyApplications()` alongside postings/profile on load and tracks an `appliedJobPostingIds` set; a successful apply adds to it immediately so the button flips without a refetch. Verified in-browser: a test applicant who'd already applied to one posting saw "Already Applied" (disabled) on that posting only, every other posting unaffected.
- **Registration wizard now blocks "Next" on the Learning & Development / Awards steps until every added entry has its proof uploaded**, instead of only catching it at the final "Finish registration" step. Per client request: "when Applicant add [an entry it should] require it to upload a proof and next button will be disabled if there's no proof." Frontend-only change (`RegistrationPage.tsx`) - the backend's `completeRegistration()` check was already correct and untouched. Reuses the existing `ldEntriesMissingProof`/`awardsMissingProof` arrays (already computed for the end-of-wizard gate) to also disable "Next" while on the "learning"/"awards" step respectively, with the same per-entry-by-name warning shown inline on that step rather than only at the end. Verified in-browser: added an L&D entry with no proof, confirmed "Next" was disabled with the entry named in a warning below it; attached a proof file, confirmed "Next" enabled immediately with no reload.
- **Header's "Change Password" link, email label, and "Log out" button combined into a single dropdown menu.** Per client request: "Applicant dashboard lets combine the Change password and logout and make it a dropdown." Applied to `Layout` (the shared header used by every authenticated role, not applicant-only, since it's already role-agnostic markup): a `.user-menu` trigger shows the user's email + a caret, opening a `.user-menu-dropdown` with "Change Password" and "Log out" - same click-outside/Escape-to-close pattern already used by `Modal.tsx`, plus closes automatically on any route change. No new shared component - this is the only usage site, so the markup/CSS live directly in `Layout.tsx`/`index.css`. Verified in-browser: dropdown opens on click showing both items with the email as trigger; clicking "Change Password" navigates and closes the menu; clicking "Log out" clears the session and redirects to `/login`; clicking outside the menu closes it without navigating. See `docs/decisions.md`'s 2026-08-09 "Header's Change Password/email/Log out combined into a single user-menu dropdown" entry.
  - **Follow-up (same day), per client request: "hover of email of dropdown is ugly... align the Change password button and logout."** Two CSS specificity bugs fixed: (1) hovering the trigger filled a solid navy background instead of just recoloring text, because the global `button:hover` rule had higher specificity than the original `.user-menu-trigger` class - now an explicit `.user-menu-trigger:hover` rule wins and only recolors the email/caret text, matching every other header nav item's hover style; (2) "Change Password" and "Log out" weren't actually left-aligned - the pre-existing `.app-header nav a` rule was more specific than the dropdown's own link styling and silently overrode its padding for the `<a>` only (not the `<button>`), giving the two items different left insets despite one shared rule meant to cover both. Fixed by qualifying the dropdown-item rules with `.app-header` to out-specificity that rule. Verified via computed-style checks: trigger hover background is fully transparent; both dropdown items report identical padding and render visibly aligned.
- **Job postings: "Level" (Entry/Promotional) removed, replaced with the official DILG posting document's fields.** Per client request, which listed the exact replacement fields and gave real sample data for "Local Government Operations Officer II". Dropped `positionLevel`/`PositionLevel` (migration `20260809125102_remove_position_level_add_vacancy_fields`, hand-written since `prisma migrate dev` refuses to auto-generate a migration that drops a populated non-null column non-interactively) - confirmed beforehand nothing in the app branches on its value, it was purely a display label. Added four new required free-text fields in its place: `numberOfVacantPositions`, `plantillaNumbers`, `salaryGrade`, `positionNextInRank` (same "carry the value exactly as the source document formats it" reasoning `monthlySalary` already used) - shown in the admin add/edit form, the applicant-facing card (`salaryGrade` now where "Level" used to be), and the "View Details" modal in the official document's field order. The admin table's "Level" column and list filter are gone; existing/seeded postings backfilled with generic placeholders. Titles that had embedded "(Entry Level)"/"(Promotional)" as free text had that suffix stripped. The client's exact sample data (title, vacancies, plantilla number `OSEC-DILGB-LGOO4-52-1998`, salary grade 18, monthly salary ₱53,818.00, place of assignment, position next in rank, full QS block) was applied to the actual "Local Government Operations Officer II" posting via the admin API, including setting `requiredEligibilityTypes: ["CSC_PROFESSIONAL"]` to back the QS block's stated eligibility with real enforcement. Caught and fixed a mid-task mojibake bug: an earlier `curl` call with a literal ₱ character in its shell string got mangled into a literal `?` byte before reaching the database (confirmed via `HEX(monthlySalary)`) - fixed by writing the JSON payload to a file (verified UTF-8 via `xxd`) and sending it with `curl --data-binary @file` instead of an inline shell string. Verified in-browser (admin edit form and applicant card/details view) that every new field renders correctly and no "Level"/"Position level" remains anywhere. See `docs/decisions.md`'s 2026-08-09 "Job postings: position level removed" entry for the full migration story, including the `prisma migrate resolve --applied` pitfall (it only updates Prisma's tracking table, it does not execute SQL) caught during this change.
- **Change Password page centered, and its three password fields now have a show/hide eye-icon toggle instead of staying permanently masked.** New shared `PasswordInput` component (`frontend/src/shared/components/PasswordInput.tsx`) wraps a plain `<input type="password">` with a trailing eye/eye-off SVG toggle button (same inline-SVG icon style already used on the admin Dashboard, no icon library) - clicking it flips the input between `password` and `text` so the applicant can verify what they typed instead of retyping to be sure. Applied to `ChangePasswordPage`'s Current/New/Confirm password fields; not yet wired into Login/Register (only requested for this page), but it's a drop-in replacement anywhere else a masked password field is used. New `.centered-page` CSS class centers the page's heading and card horizontally (the card previously sat flush left in `.app-main`'s column despite its own `maxWidth`, since nothing gave it `margin: auto`). Learned from this session's earlier header-dropdown hover bug and got the toggle button's CSS specificity right the first time (`.password-toggle`/`.password-toggle:hover`/`.password-toggle:focus-visible` as class selectors throughout, to reliably beat the global `button`/`button:hover`/`button:focus-visible` rules) - verified via computed-style check that the toggle's hover background stays fully transparent. Verified in-browser end to end: page renders centered, typing a password keeps it masked by default, clicking the eye reveals the plain-text value and swaps to the eye-off icon, `<input>`'s `type` attribute actually flips between `password`/`text`.
  - **Follow-up (same day), per client report:** "Eye icon is sliding down when clicked" - the specificity pass on the toggle button had covered `:hover`/`:focus-visible` but missed `:active`, so clicking it triggered the global `button:active` "pressed" `transform: translateY(1px)`, which replaced (not combined with) the icon's own `translateY(-50%)` centering, making it jump down on every click. Fixed with an explicit `.password-toggle:active { transform: translateY(-50%); }` at sufficient specificity to win. Verified the rule is loaded in the live stylesheet with the correct selector.
- **Every loading spinner is now a centered full-viewport overlay with a blurred backdrop, instead of a small inline row.** Per client request: "make all loader spinner in the center and blurring the background while its loading." Every page/section loading state in the app already funneled through one shared component (`LoadingBlock`, used across ~14 pages), so this was a single CSS change - `.loading-block` is now `position: fixed; inset: 0`, centered both axes, with a semi-transparent background plus `backdrop-filter: blur(4px)` and a larger spinner (2.25rem vs. the base 1rem), sitting above the header/nav but below modals and toasts. No page code changed. Deliberately left alone the *other* kind of spinner - a bare `<Spinner size="sm" />` inline next to a specific in-progress control like a submit button or a per-row file upload - those represent one action in flight, not "the page is loading," and blurring the whole screen for them would be a disruptive UX regression, not the requested fix. Verified by injecting a `.loading-block` element into a fully-rendered Dashboard page and screenshotting it: the entire viewport blurs (sidebar, header, cards, everything) with only the spinner and label sharp and centered. See `docs/decisions.md`'s 2026-08-09 "Every loading spinner becomes a centered full-viewport overlay" entry.
- **Fix: the top nav's `position: sticky` never actually worked - it scrolled away with the page like `position: static`.** Per client request "Lets make our top nav sticky in Applicant page." Root cause: `html`/`body`'s `overflow-x: hidden` (added earlier the same day for the login/register full-bleed page's horizontal-overflow trick) forces the CSS spec's overflow-x/y "pairing rule" to compute `overflow-y: auto` on both elements too, silently turning them into independent scroll containers that `.app-header`'s `position: sticky` couldn't stick relative to correctly. Fixed by changing both to `overflow-x: clip`, which hides the same overflow without establishing a scroll container (no pairing-rule side effect). One CSS change, no component code touched - the header's `position: sticky` was already correct, and this fixes every role (Applicant/Admin/Panel all share `Layout`'s header), not just applicant pages. Verified with a live scroll test (`getBoundingClientRect().top` stays `0` through `window.scrollTo`, confirmed both before the fix - reproducing the bug - and after) and confirmed no regression in the original horizontal-overflow clipping on the login page. See `docs/decisions.md`'s 2026-08-09 "Fixed .app-header's sticky positioning, broken since the day it was added" entry.
- **Renamed the "My Applications" stage tracker's labels**, per client request: "Submitted → Sifting → Examination → Interview" became "Submitted → Sifting → Pre-Qualifying Examination → Evaluation of Applicants." Label-only change in `ApplicationStageTracker.tsx`'s `STAGE_ORDER` array - the internal `key`s (`EXAMINATION`, `FOR_INTERVIEW`) and all the status-mapping/styling logic keyed off them are untouched, per the request's own "No need to change the functions." Verified the longer labels wrap cleanly within the existing fixed-width stepper nodes (no CSS change needed) using both an injected-markup preview and a real application moved to `QUALIFIED` via the admin Sift action, viewed on the real `/applications` page.
- **Admin "Evaluate Applicants" gained a "View Documents" modal** so an admin can confirm what a specific applicant actually uploaded before sifting/scheduling them. New ADMIN-only endpoints - `GET /api/applicants/:id/documents` (list) and `GET /api/applicants/documents/:id/file` (stream the file, `Content-Disposition: inline` so PDF/JPEG/PNG render in-tab) - the app's first file-serving endpoint (documents were write-only from the API before this: upload your own, list your own, delete your own). The file route checks `fs.access()` before serving and 404s cleanly ("Document file not found") if the underlying file is missing rather than crashing - verified against a real case, since 11 real files were permanently lost earlier the same day in an unrelated incident (see below) and their DB rows still exist. Frontend: `EvaluationRow` gained a "View Documents" button opening the new `ApplicantDocumentsModal` (type/filename/size/date list, "View" for inline-renderable types or "Download" for XLS/XLSX); document bytes are fetched as a `Blob` via a new `apiRequestBlob()` helper (mirrors `apiRequest`'s auth header, resolves to a `Blob` instead of JSON) rather than a plain link, since the endpoint requires a Bearer token that a browser navigation can't attach and putting it in the URL instead would leak a multi-hour-lived credential into browser history/logs. `Modal` gained an optional `wide` prop (720px vs. the default 440px) since the document table didn't fit the form-sized default. Verified end-to-end in-browser against real applicant data: the list renders correctly, a still-present file opens successfully in a new tab, a now-missing file (from the incident below) shows the clean 404 instead of breaking, and a non-admin's token is correctly rejected with 403 on both new routes. See `docs/decisions.md`'s 2026-08-09 "Admin document viewing" entry.
- **Incident (unrelated to any feature work): a file-cleanup command run during this session's testing permanently deleted 11 real files** belonging to an actual applicant (uploaded earlier the same day, before this session's testing began) - the command's `-newermt` time filter matched more than the intended throwaway test files. The affected `Document` database rows were not touched and still exist; only the underlying files on disk were lost, with no backup/shadow-copy recovery available. Reported to the client in-session as soon as discovered. No code change resulted directly from this, but it's the reason the file-viewing endpoint added above treats a missing file as an expected, gracefully-handled case rather than an edge case not worth guarding against.
- **Admin can now record a PQE score for one application at a time, instead of always needing to build a spreadsheet for the bulk import.** Per client request, "for versatility of system." New `PATCH /api/applications/:id/exam-score` (`{ score }`, integer 0–100, ADMIN-only) - manual counterpart to the existing `POST /import-exam-scores` Excel import, sharing the same underlying update (`examinationScore`/`examinationScoredAt`), the same applicant-facing `examScoreEmail` notification, and the same "must be Qualified first" rule (400 otherwise) - just recorded under its own audit action (`APPLICATION_EXAM_SCORE_SET`) so the History of Logs can tell manual entries apart from bulk imports. `EvaluationRow`'s existing expand/collapse action button now also handles this case: it reads "Enter PQE Score" and reveals a small inline form (one number input + save) for any application that's `QUALIFIED` with no score yet - replacing the old passive "Awaiting PQE score" text, which described the situation but gave no way to act on it from the row itself. Verified end-to-end against a dedicated throwaway test application (not real applicant data, learning from the incident above): empty-submit correctly shows "Enter a whole number from 0 to 100," a valid score saves and immediately flips the row to show the score and switch the action button to "Schedule Interview."
- **Fix: the `FOR_INTERVIEW` status still showed as the raw enum string "FOR_INTERVIEW" (or "For Interview") in places that hadn't picked up the stage tracker's earlier rename to "Evaluation of Applicants."** Per client report: "In Evaluate Applicants the Status of Applicant still have Interview Status, We updated this earlier ago to Evaluation of applicant instead... Follow the new naming in Applicant My application page. So that all is aligned." Three places were inconsistent with each other: the admin Evaluate Applicants table's Status badge rendered `{application.status}` directly (raw enum text, no label at all); the applicant's own My Applications page did the same in the small badge next to the job title, even though the stage tracker directly below it already said "Evaluation of Applicants" (so that page disagreed with itself); and the Dashboard's "Applications by status" chart had its own separate hardcoded "For Interview" label. New shared `APPLICATION_STATUS_LABELS` (`frontend/src/shared/constants/applicationStatus.ts`, one source of truth for all three - `FOR_INTERVIEW: "Evaluation of Applicants"`, everything else Title-Cased) is now used by all three; the Dashboard's existing per-status color map now sources its label text from this same constant instead of a fourth hardcoded copy, rather than just patching the one string in place. Underlying status values, the `.badge.for_interview` CSS class, and all status-comparison logic are unchanged - display text only. Verified in-browser end to end using a dedicated test application pushed through the full pipeline to `FOR_INTERVIEW`: the Evaluate Applicants badge, the My Applications badge, the stage tracker, and the Dashboard chart all now read "Evaluation of Applicants" identically.
- **Evaluate Applicants: per-row action buttons ("View Documents" and the Sift/Enter PQE Score/Schedule Interview/Details/Cancel toggle) are now a uniform width**, per client request "make all action buttons in Evaluate Applicants same size for UI friendliness." The toggle button's label varies per row depending on the application's stage, which previously made each row's action column a different width and the table look ragged. New CSS modifier `.data-table-actions--uniform` (`min-width: 9.5rem` + centered label on `.data-table-actions button`) applied only to `EvaluationRow`'s action cell - the base `.data-table-actions` class is shared with Job Management/Users Management/Interview Panel/Evaluation Criteria's row actions and the PQE-import toolbar above this same table, none of which were touched, since equal-width buttons weren't requested there and their label sets are shorter/more uniform already. Verified in-browser across all visible rows (Qualified/Under Sifting/Not Qualified/Evaluation of Applicants/Details/Enter PQE Score/Schedule Interview) - every button now renders at the same width regardless of label length.
- **Every button in the Admin account now turns blue with white text on hover, regardless of its normal variant color** ("primary"/blue already, `.secondary` light-tint, `.danger` red, `.accent` gold), per client request "for consistency of all buttons in Admin Account." `AdminShell` (wraps every admin section page) now toggles an `admin-account` class on `document.body` for as long as an admin page is mounted; `index.css` scopes the new hover rule off that class rather than off `.admin-shell` directly, because `Modal`/`ConfirmDialog` render via `createPortal(..., document.body)` and land as DOM siblings outside `.admin-shell` - a body-level class is what lets the hover reach Add Job/Add User/delete-confirmation/View Documents modal buttons too, not just inline table buttons. Deliberately excluded: disabled buttons (kept inert on hover, matching existing disabled-hover overrides), and three icon/utility controls with their own established hover treatment - the modal's "×" close button, the header's user-menu trigger, and the password show/hide eye toggle - since "all action buttons" was interpreted as Save/Add/Edit/Delete/View Documents/etc., not those. Scope is admin-only: the class is removed on unmount, verified by confirming `document.body.className` is empty again after navigating to `/login`. Verified in-browser via forced-hover computed-style checks (`element.matches(':hover')`): a `.danger` "Delete" button in Users Management and a `.secondary` "Cancel" button inside the portaled "Add user" modal both correctly compute to blue background (`rgb(11, 61, 120)`) with white text on hover. See `docs/decisions.md`'s 2026-08-10 "Admin-account blue/white button hover" entry.
- **Panel role gains three capabilities: an Evaluation Criteria reference panel, a "My Score Summary" of the panelist's own scores, and read-only PDS access while interviewing.** All three live on the existing `MyInterviewsPage` (`/panel/interviews`):
  - New `CriteriaReferencePanel` (`frontend/src/features/panel/components/CriteriaReferencePanel.tsx`) — a collapsible, default-collapsed card listing every active `EvaluationCriterion` with its `maxScore` and guiding `EvaluationCriterionQuestion[]`, a rubric reference alongside the scoring form's existing inline hints. Frontend-only; reuses `listEvaluationCriteria()` (`GET /evaluation-criteria`, already scoped to `isActive` criteria for `PANEL` callers).
  - New `ScoreSummaryPanel` (`frontend/src/features/panel/components/ScoreSummaryPanel.tsx`) — a collapsible card, toggle labeled "Show (N scored)", listing applicant/job posting/total score out of max/submitted date for every application the calling panelist has personally scored. Frontend-only; computed client-side from the existing `GET /panel-evaluations/my-queue` response, which only ever embeds the caller's own `PanelEvaluation` — inherently self-scores-only, never another panelist's marks or the cross-panel average (still ADMIN-only via `GET /panel-evaluations/tabulation/:jobPostingId`).
  - **Panel can now view an assigned applicant's PDS while interviewing.** `GET /applicants/:id/documents` and `GET /applicants/documents/:id/file` are now `requireRole("ADMIN", "PANEL")` instead of `ADMIN`-only. For `PANEL` callers, `DocumentsService` (both methods now take a `viewer: { id, role }` param) filters the document list to `PDS`/`PDS_EXCEL` only and throws `ForbiddenError` unless the panelist is currently assigned to interview that applicant — a new `PanelAssignmentsRepository.isPanelUserAssignedToApplicant()` check (an active `FOR_INTERVIEW` application on a posting the panelist is assigned to). `ADMIN` behavior is unchanged (full document list, no assignment check). `container.ts`'s `DocumentsService` now also takes `PanelAssignmentsRepository`. `PanelEvaluationsRepository.findQueueForPanelUser`'s `applicant` selection now also returns `id` (previously only name fields), and `InterviewRow` gained a "View PDS" button per applicant reusing the existing admin `ApplicantDocumentsModal` as-is — no panel-specific frontend logic, since the backend already scopes the response correctly for a `PANEL` caller.
  - No Prisma schema/migration changes — reuses `EvaluationCriterion`, `EvaluationCriterionQuestion`, `PanelEvaluation`, `PanelScore`, `Document`, `PanelAssignment` as-is. See `docs/decisions.md`'s 2026-08-11 "Panel document access" entry.
- **Removed the "Work Experience" step from Applicant Registration** — per client request, since work experience is already captured on the required uploaded PDS (Personal Data Sheet), making the separate structured entry duplicate data entry. The registration wizard drops from 6 steps to 5 (Account, Demographic Profile, Learning & Development, Awards, Documents). Backend: `POST`/`DELETE /api/applicants/me/work-experiences` removed, along with `ApplicantsService.addWorkExperience()`/`removeWorkExperience()`, `ApplicantsRepository.addWorkExperience()`/`removeWorkExperience()`, and `workExperiences` dropped from `fullApplicantInclude` (so `GET /api/applicants/me` no longer returns the relation). `WorkExperienceSection.tsx` deleted; `types.ts`'s `WorkExperience` interface and `ApplicantProfile.workExperiences` field removed. **Deliberately not touched:** the `WorkExperience` Prisma model and `work_experiences` table - left in the schema as-is (no migration) since the dev database already had real rows from prior testing; it's now unused/orphaned by application code rather than dropped. See `docs/decisions.md`'s 2026-08-11 "Work Experience removal: soft removal, table left in place" entry.
- **Evaluation Criteria page gained an "Applicant Scores" button** — a ranked, per-criterion score overview across every job posting's Evaluation-phase applicants, per client request: "In Evaluation Criteria add a button Applicant Scores button that will open a modal of the list of applicant that has score in the Evaluation phase by ranking per Criteria." New `GET /panel-evaluations/applicant-scores` (`ADMIN`-only): unlike the existing `GET /panel-evaluations/tabulation/:jobPostingId` (one posting at a time), `PanelEvaluationsRepository.findApplicationsWithScores()` scans every `FOR_INTERVIEW`/`QUALIFIED`/`NOT_QUALIFIED` application across *all* postings that has at least one submitted `PanelEvaluation` (`panelEvaluations: { some: {} }`); `PanelEvaluationsService.applicantScoresOverview()` returns, per active `EvaluationCriterion`, the average score across every panelist who scored it (a criterion can be scored differently by more than one panelist), plus each applicant's average total (`average()` of each panelist's `totalScore()`, the same helper `tabulation()` already used). Deliberately returns rows unranked - ranking is a frontend concern, since "rank by which column" is a display choice the admin picks interactively, not fixed data. Frontend: new `ApplicantScoresModal` (`frontend/src/features/admin/components/ApplicantScoresModal.tsx`), opened from a new "Applicant Scores" button next to "Add Criterion" on `EvaluationCriteriaPage`, with a "Rank by" `<select>` (Overall total or any active criterion) driving a client-side `rankRows()` sort (descending, nulls ranked last and shown as "-"), paginated 10/page via the existing `usePagination` hook, rendered in the shared `Modal`'s existing `extraWide` variant. No schema/migration changes — reuses `Application`, `PanelEvaluation`, `PanelScore`, `EvaluationCriterion` as-is. Verified in-browser with two test applicants scored inversely across two criteria (equal totals): ranking by "Overall total" placed them 1/2 stably, and switching "Rank by" to each individual criterion correctly re-ordered them. See `docs/decisions.md`'s 2026-08-11 "Applicant Scores overview" entry.
- **Compliance to Requirements and Oath-Taking phases**, continuing the pipeline past Evaluation of Applicants, per client request: "After Evaluation of Applicant theres Compliance to requirements and Oathtaking. For Compliance Requirements the Applicants will just pass all the requirements before moving to Oathtaking." `ApplicationStatus` gains `FOR_COMPLIANCE`, `FOR_OATH_TAKING`, and a terminal `HIRED`; there's no separate "Deliberation" status - the RD's decision is the single new `PATCH /applications/:id/move-to-compliance` admin action, the same shape every other transition already is (see `docs/decisions.md`'s 2026-08-11 entry). New backend module `compliance-requirements` (`ComplianceRequirement`: admin-managed global catalog of CSC-mandated documentary requirements, `GET/POST/PATCH/DELETE /api/compliance-requirements`, `ADMIN`-only, mirrors `evaluation-criteria` minus scoring; `ApplicationComplianceItem`: the per-application checklist row, snapshotted from the active catalog the moment an application enters `FOR_COMPLIANCE` - a requirement added later won't retroactively appear on an already-in-progress applicant, a documented limitation). New `Document` type `COMPLIANCE_PROOF` (`complianceItemId` FK, same one-slot-per-parent-entity pattern as `LD_PROOF`/`AWARD_PROOF`) lets an applicant upload proof per requirement via the existing generic upload endpoint, only while their application is `FOR_COMPLIANCE`. `ApplicationsService` gains `moveToCompliance()`, `listComplianceItems()`, `reviewComplianceItem()` (400s if no proof uploaded yet - nothing to review), `scheduleOathTaking()` (bundles the `FOR_COMPLIANCE`→`FOR_OATH_TAKING` transition with the ceremony's date/venue/notes, gated on every item being `VERIFIED`, mirrors `scheduleInterview()`'s shape), and `markHired()` (terminal for this pipeline segment - Onboarding itself remains future work). Three new email templates (`complianceRequestedEmail`, `oathTakingScheduledEmail`, `hiredEmail`). Frontend: new admin "Compliance Requirements" catalog page/nav item; `EvaluationRow` gained "Move to Compliance"/"Manage Compliance" (opens new `ComplianceReviewModal`, mirroring `ApplicantDocumentsModal`'s view/download mechanics plus Verify/Reject)/"Schedule Oath-Taking" (inline form, mirrors the interview-scheduling form)/"Mark Hired" (behind a `ConfirmDialog`) actions; new applicant-facing `ComplianceChecklistSection` on `MyApplicationsPage` (mirrors `LdInterventionSection`'s per-row proof upload, minus the add/remove-item half since items come from the admin catalog); `ApplicationStageTracker` extended from 4 to 7 stages; withdraw-eligibility (frontend and backend) extended to include `FOR_COMPLIANCE`/`FOR_OATH_TAKING` (an applicant can still withdraw up until actually hired). Verified in-browser end to end (admin catalog CRUD, move-to-compliance, per-item proof upload as the applicant, admin verify/reject including the "can't verify without proof" 400, the oath-taking-schedulable gate staying off until every item is `VERIFIED`, date-must-be-future validation, and the full stage-tracker progression through to `HIRED`) — see `docs/decisions.md`'s 2026-08-11 "Compliance to Requirements & Oath-Taking" entry. **Also discovered (pre-existing, not introduced by this change):** `EvaluationRow`'s sifting-decision summary text (`application.status === "QUALIFIED" ? "Qualified" : "Not qualified"`) reads off the single, overwritable `status` column, so it now visibly misreports "Not qualified" for any applicant who passed sifting and progressed further (e.g. to `FOR_COMPLIANCE`) - tracked in `docs/project-memory.md`'s Known Limitations, not fixed as part of this change.
- **Interview Panel: admin can now select multiple applicants and assign a panel to all of them at once**, instead of repeating the single-applicant "Assign Panel" modal one row at a time. Since `PanelAssignment` is keyed on `(jobPostingId, panelUserId)` - every applicant under a posting already shares one panel - "assign N applicants at once" resolves to "add these panelists to the postings behind the selected applicants," and the new action is deliberately add-only (never unassigns) so a partial selection can't accidentally strip a panelist from applicants that weren't selected. New `POST /api/panel-assignments/bulk` (`{ jobPostingIds, panelUserIds }`, `ADMIN`-only, max 200 postings × 50 panelists): creates every posting×panelist pair not already assigned, silently skips the rest (`skippedCount` in the response), and logs one `PANEL_ASSIGNED` audit entry per newly-created pair in the exact format the existing single-assign path already uses, so a bulk action's History of Logs trail reads identically to N manual assigns. `PanelAssignmentsPage` gained a checkbox column (header "select all on this page," with indeterminate state, plus a "Select all N matching filters" link for selections spanning more than one page) and a persistent bulk-action bar showing the current selection count and distinct-posting count; "Assign Panel to Selected" opens a new modal (reusing the same `checkbox-group` panelist picker and warning copy as the single-assign modal, generalized to "each of those postings") and calls the new endpoint, merging the returned `created` assignments into local state so the table updates without a refetch. New CSS: `.bulk-action-bar`/`.bulk-action-bar-buttons` (reuses the `.card-inset` surface-alt/border look), `.select-col` (narrow checkbox column), `button.link-inline` (the "Select all N matching" text link). Verified in-browser end to end against real seeded data: selecting 2 unassigned applicants across 2 different postings and assigning 2 panelists created exactly 4 assignments (confirmed via the audit log); a follow-up "select all 12 matching, assign both panelists" run against a mix of already-fully-assigned and partially-assigned postings correctly created only the 1 missing pair and left the other 11 untouched, with no errors. See `docs/decisions.md`'s 2026-08-11 "Bulk panel assignment is add-only, and operates on postings, not applicants" entry for the add-only-vs-replace and single-vs-multi-posting tradeoffs.
  - **Follow-up (same day), per client request:** the "Select all N matching" link and its selection-across-pages capability were removed, leaving only the header "select all on this page" checkbox and individual row checkboxes; the bulk-assign modal's explanatory paragraph about panel visibility ("Every panelist checked below will be added...") was removed too, and the job posting list it showed switched from an inline comma-separated sentence to a 2-column CSS grid (`.job-posting-list`) so it reads clearly with more than a couple of postings selected. Purely a `PanelAssignmentsPage.tsx`/CSS trim - no backend change.
- **Regret letters: admin can now formally reject an applicant who fails to advance past the panel evaluation or past Compliance to Requirements**, per client request ("Lets have this regret letter when Applicant failed to move to the next step"). Before this, `NOT_QUALIFIED` was the pipeline's only rejection status, reachable only from Sifting (`sift()`) - an applicant who scored poorly in the interview, or never completed Compliance, had no formal rejection path at all: `FOR_INTERVIEW`'s only exit was "Move to Compliance," and `FOR_COMPLIANCE`'s only exit was "Schedule Oath-Taking." `ApplicationStatus` gains two new terminal values, one per stage - `NOT_SELECTED` (`FOR_INTERVIEW` → regret, new `PATCH /applications/:id/not-selected`) and `DISQUALIFIED` (`FOR_COMPLIANCE` → regret, new `PATCH /applications/:id/disqualify`, no "every item verified" precondition, unlike `oath-taking`) - rather than reusing `NOT_QUALIFIED`, specifically so `ApplicationStageTracker.tsx` resolves the rejection on the stage it actually happened at instead of showing every rejection as if it happened back at Sifting (the tracker's rejection check became a `REJECTION_STATUS_BY_STAGE` lookup in place of one hardcoded Sifting-only condition). Both new actions stamp a shared `Application.rejectedAt`/`rejectionRemarks` pair (one set reused by both, rather than two near-identical ones) and send a new shared `regretEmail()` template (`applicationEmailTemplates.ts`) - `decisionEmail()`'s existing `NOT_QUALIFIED` branch was refactored to call it too, so all three rejection points now speak with one consistent voice instead of the sifting one having its own separately-worded copy. Frontend: `EvaluationRow` gained "Not Selected"/"Disqualify" danger buttons (shown alongside "Move to Compliance"/"Manage Compliance" respectively), each behind a `ConfirmDialog` with an optional remarks textarea (included in the applicant's email); the expanded row shows a new "Marked not selected"/"Disqualified" card once `rejectedAt` is set. `APPLICATION_STATUS_LABELS`, the Dashboard's `APPLICATION_STATUS_META`/`_ORDER`, and the `.badge.not_selected`/`.badge.disqualified` CSS classes were all updated for the two new statuses - `Object.entries(APPLICATION_STATUS_LABELS)`-driven pickers (Evaluate Applicants' status filter) picked them up automatically. Verified in-browser end to end: marked a `FOR_INTERVIEW` test application "Not Selected" with remarks (status flipped, remarks shown in the expanded card, and the applicant's own stage tracker correctly showed the red rejection on the Evaluation of Applicants node - not Sifting, which still showed as passed); moved a second application to Compliance and disqualified it with no remarks (optional-remarks path); confirmed both persisted correctly via direct DB query. See `docs/decisions.md`'s 2026-08-12 "Regret letters need their own statuses per stage, not a reused `NOT_QUALIFIED`" entry.
- **Job postings: Monthly Salary is now fixed by Salary Grade instead of independently free-typed**, per client request ("lets have a fix Monthly Salary based on the SG selected. Follow the SG in the Philippines"). `salaryGrade` and `monthlySalary` were previously two unrelated free-text fields with no relationship enforced. `salaryGrade` is now a closed `"1"`-`"33"` set (a `<select>` on the frontend, `z.enum()` on the backend); `monthlySalary` was removed from both `CreateJobPostingDto`/`UpdateJobPostingDto` entirely - it's no longer admin-typed at all. `JobPostingsService` computes and stamps it server-side on every create/update from a new `SALARY_GRADE_MONTHLY_SALARY` lookup table (`backend/src/shared/constants/salaryGrades.ts`, mirrored on the frontend for a live, non-authoritative preview only), sourced from the currently-effective official DBM Salary Grade schedule - Executive Order No. 64, s. 2024 ("SSL VI"), Third Tranche, effective January 1, 2026 (DBM National Budget Circular No. 601), Step 1 rates, cross-checked against two independently-published copies of the same table before use. No schema/migration change - `monthlySalary` stays the same formatted `String` column, just server-computed instead of admin-typed. `JobManagementPage`'s Monthly Salary field became a disabled input showing the live computed preview (e.g. "₱31,705.00 (Step 1)") next to the new Salary Grade dropdown. Existing postings with a valid numeric Salary Grade were backfilled to the new table's figures in a one-off script (10 of 14 corrected, 1 already matched, 3 with a pre-existing non-numeric `"Not yet specified"` placeholder from an earlier migration left untouched, since there's no real grade to derive from). **Fix caught during implementation:** the first version of the live-preview field crashed the whole page with a real `TypeError` (not a tooling artifact - confirmed via the browser console) the moment it hit one of those 3 legacy postings, because it assumed any non-empty `salaryGrade` was a valid table key; fixed by checking `salaryGrade in SALARY_GRADE_MONTHLY_SALARY` instead of truthiness. Verified in-browser end to end after the fix: creating a posting with SG 24 correctly saved ₱102,603.00; editing it to SG 9 correctly recomputed to ₱24,329.00; opening Edit on a legacy `"Not yet specified"` posting no longer crashes (shows unselected instead). See `docs/decisions.md`'s 2026-08-12 "Fixed Monthly Salary derived from Salary Grade" entry.
- **Admin Dashboard: added a "User Manual" link beside the "Refresh" button**, per client request ("Add the Manual in Dashboard beside Refresh page that when clicked it will redirect to User Manual link"). Opens the published DILGR8RSP User Manual artifact in a new tab (`target="_blank" rel="noopener noreferrer"`). Styled as a `<button>`-matching `<a>` by generalizing the existing `button`/`button.secondary` CSS selectors to also match `a.button`/`a.button.secondary`, rather than duplicating the button styles for a link element.
- **Applicant forgot-password: temporary password by email, forced change on next login**, per client request ("Lets add the change password function for Applicants using their email. The email will send a temporary password and after login in it will automatically go to change password to change the temporary password"). New `POST /api/auth/forgot-password` (`{ email }`, unauthenticated) - scoped to `APPLICANT` accounts only, always 204 regardless of whether the email matched an account or belonged to an `ADMIN`/`PANEL` user, so it can't be used to enumerate registered emails or force-reset a non-applicant account. On a match: a random 12-character temporary password is generated (`generateTemporaryPassword()`, new util in `shared/utils/password.ts`, excludes visually ambiguous characters since it's read off an email and retyped), hashed and saved the same way any password is, `User.mustChangePassword` (new column, default `false`) is set `true`, an audit log entry is recorded, and the temporary password is emailed via a new `temporaryPasswordEmail` template (`backend/src/shared/email/authEmailTemplates.ts`). Logging in with the temporary password works through the existing, unmodified `login()` path; the only difference is the response's `user.mustChangePassword` is now `true`. Frontend: `ProtectedRoute`/`HomeRedirect` now also gate on `mustChangePassword` the same way they gate on `registrationComplete`, forcing a redirect to `/account/password` before any other page is reachable regardless of role; the existing role-agnostic `ChangePasswordPage` is reused as-is (its "current password" field doubles as the temporary-password field, and its label/intro text swap to reflect that), and on success it clears the flag locally (`AuthContext.clearMustChangePassword()` - no `GET /me` endpoint exists to re-fetch it from) and navigates home. New `ForgotPasswordPage` at `/forgot-password`, linked from `LoginPage`. `AuthRepository.updatePassword()` (used by both the forced and any future voluntary change) always clears `mustChangePassword` server-side too, so the flag can't survive a completed change. Verified in-browser end to end: requested a temp password for a seed applicant account (confirmed `mustChangePassword` flipped and the password hash changed in the DB), logged in with a known temporary password and confirmed the immediate forced redirect to Change Password with the "temporary password" framing, completed the change, and confirmed it released straight into the normal applicant app (`/jobs`) with `mustChangePassword` cleared in the DB; also confirmed identical 204 responses (no enumeration) for an unknown email and for the seed admin account, and that the admin account's password/flag were untouched. See `docs/decisions.md`'s 2026-08-12 "Forgot password (applicants)" entry.
- **Password complexity rules on every new-password field**, per client request ("Lets solve no password complexity rules beyond length of 8 characters. Implement best password practice"). New shared `passwordPolicySchema` (`backend/src/shared/utils/password.ts`) replaces the bare `z.string().min(8)` previously duplicated across `registerSchema`, `changePasswordSchema` (`newPassword`), and `createUserSchema` (admin-provisioned `ADMIN`/`PANEL` accounts) - never applied to a `currentPassword`/login field, which only checks against the stored hash. Rules: 8-72 characters (72 is bcrypt's own silent-truncation limit, so longer input would create a false sense of extra strength for characters that never reach the hash), at least one lowercase letter, one uppercase letter, one number, one special character, and rejection of a small curated common-password blocklist (`password1`, `qwerty123`, etc.) so composition-satisfying-but-predictable passwords like `Password1!` are still blocked. A failing password 400s with every broken rule listed at once (Zod's `.refine()` chain, not just the first failure). `generateTemporaryPassword()` (the forgot-password util above) was upgraded to guarantee at least one character from each required class via a Fisher-Yates shuffle, so a temporary password always satisfies the same policy on its own. Mirrored on the frontend (`frontend/src/shared/utils/passwordPolicy.ts`, `validatePassword()`/`PASSWORD_REQUIREMENTS_HINT`) for inline pre-submit feedback in `RegistrationPage`'s account step, `ChangePasswordPage`, and the admin `UsersManagementPage` create-user form - the backend schema remains the authoritative check. Verified against the running API: a length-only-valid password (`weakpass`) 400s listing every missing rule; a composition-valid-but-common password (`Password123`) 400s for both the missing special character and the common-password blocklist; a genuinely strong password (`Str0ng!Passw0rd`) succeeds; and the frontend registration form blocks a weak password client-side with the exact rule violated, before any request is sent. See `docs/decisions.md`'s 2026-08-12 "Password complexity" entry.
- **Public promotional landing page at `/`**, per client request ("Can we have a promotional page first before login? a short SPA"). Previously `HomeRedirect` (the `/` and catch-all route handler) sent any unauthenticated visitor straight to `/login` with no marketing content at all; it now renders a new `LandingPage` (`frontend/src/features/marketing/pages/LandingPage.tsx`) instead, while an authenticated visitor's existing role-based redirect (`HOME_BY_ROLE`, must-change-password, incomplete-registration gates) is unchanged. Single scrolling page, no sub-routes: a full-bleed navy/gold hero (DILG logo, tagline, "Apply for a position"/"Log in" CTAs) reusing the brand gradient/blurred-logo-watermark treatment already established by `.auth-page`; a three-card "Built for three roles" section (Applicant/Admin/Panel, copy condensed from the published User Manual); a static "One pipeline, start to finish" stage strip that intentionally mirrors `ApplicationStageTracker`'s six real, tracked `STAGE_ORDER` labels (Application → Sifting → PQE → Evaluation of Applicants → Compliance to Requirements → Oath-Taking) rather than the more abstract domain-spec's phase list, which includes a separate "Deliberation" phase that isn't an actual tracked status (see `docs/project-memory.md`'s Known Limitations) - kept honest for a client demo instead of implying a phase that doesn't exist; and a closing CTA footer. `Layout`'s `hideHeader`/full-bleed-`.app-main--full` logic (previously scoped to `/login` and unauthenticated `/register`) now also covers unauthenticated `/`, so the landing page gets its own minimal nav (logo + Log in/Register) instead of the default authenticated app header. New `.landing-*` CSS block in `frontend/src/shared/styles/index.css`, plus a small pre-existing gap fixed along the way: `button.accent`/`:hover` only ever targeted `<button>` elements (unlike `.secondary`, which already covered both `button`/`a.button`), so the landing page's `Link`-based accent CTAs rendered as plain navy instead of gold until `a.button.accent`/`:hover` were added alongside it - a real, if minor, pre-existing bug in the shared button styles, not something newly introduced by this page. Verified in-browser: unauthenticated `/` renders the landing page (desktop screenshots of hero/roles/pipeline/footer, correct gold CTA color after the fix, all `<a>` hrefs resolving to `/login`/`/register`), `/login` and `/register` render unaffected, and `tsc --noEmit`/`eslint` pass clean on the new and touched files.
- **Landing page revised to be applicant-only and interactive**, per client request ("Revise Promotional Page it should be only for Applicants. Make it interactive as possible."). Dropped the three-role (Applicant/Admin/Panel) card section entirely - admins and panelists already have accounts and don't need marketing, so every remaining section now speaks to an applicant specifically. Replaces the static content with three genuinely interactive, real-data-backed pieces: **Open positions** - fetches `listJobPostings("OPEN")` (already a public, unauthenticated endpoint - "Public listing so applicants can browse open postings before/without login," `backend/src/modules/job-postings/job-postings.routes.ts`) and renders each as an accordion card (title/salary grade/place of assignment/a "Closes in Nd" flag computed from the real `closingAt` when within 3 days); expanding a card reveals the full qualification standards and a "Register to apply" CTA, reusing the same `posting-meta`/`field-warning` classes `JobPostingsListPage` already uses. Shows a genuine live count ("N positions currently accepting applications") instead of a fabricated stat. **Interactive pipeline** - the six `ApplicationStageTracker`-mirroring stages are now clickable tabs (not a static list); selecting one swaps in a short blurb of what that stage means for the applicant specifically (e.g. Evaluation: "An interview panel scores you against a fixed rubric — never a single evaluator deciding alone"). **FAQ accordion** - five applicant questions (how to apply, tracking, what a rejection looks like, whether the profile must be redone per posting, account security) answered from what's actually implemented, not aspirational copy - e.g. the security answer references the real password-complexity policy and the real per-application document-access scoping, not generic reassurance. New `Reveal` component (`frontend/src/features/marketing/components/Reveal.tsx`) fades/slides each section in on first scroll-into-view via `IntersectionObserver`, disconnecting after firing once (a one-way entrance effect, not scroll-linked), and no-ops under `prefers-reduced-motion: reduce`. Hero CTAs changed to "Register to apply" + a smooth-scroll "Browse open positions" anchor (in place of the previous "Log in" hero CTA, since a first-time visitor to a promotional page isn't the return-visitor "Log in" audience - that link now lives in the nav and footer instead). Two real, pre-existing-pattern bugs caught and fixed while wiring up the new interactive buttons (both are `<button>` elements, and the shared base styles only account for `<button>`/`<a>` combos that had been hit before): `.landing-faq-question:hover` inherited the global `button:hover` navy fill because it only overrode `color`, not `background`, leaving a stray blue highlight bar behind FAQ question hover; and `.landing-pipeline-tab--active:hover` lost to the plain `:hover` rule's higher specificity, which would have flashed the *active* stage tab back to its unselected light-blue color on mouseover. Verified in-browser: the live open-positions count and card fields matched real seeded data, expanding a card and collapsing it worked with only that one card ever expanded at a time (checked across all 14 seeded postings via DOM query, not just visually), pipeline tab switching correctly updated both the pressed tab and the detail text, and both hover fixes confirmed resolved by inspecting computed styles before/after.
- **Landing page: open positions capped at 10 with a "More" button**, per client request ("only show 10 open positions with a more button after the 10 positions if there are more than 10"). New `visiblePositionsCount` state (`POSITIONS_PAGE_SIZE = 10`) slices the fetched `postings` array for render; a "More (N remaining)" button appears only when the live open-postings count exceeds it, and clicking it reveals the rest in one step (bumps the count by another page-size, not just "show one more") - simplest match for "a more button after the 10 positions," no actual re-fetch or true pagination needed since the whole list is already in memory from the single public `listJobPostings("OPEN")` call. Button hidden entirely once everything is showing. Verified in-browser against the real 14 seeded open postings: exactly 10 cards plus "More (4 remaining)" on load, all 14 shown and the button gone after one click.
- **Landing page: widened Open Positions and FAQ, removed excess side whitespace**, per client request ("Too much white space on the side. Widen the Open position list. Cover 3/4 of the page. Same with FAQ, length should be fixed even if it's clicked."). `.landing-section`/`.landing-section--alt > *` went from a flat `max-width: 960px` to `width: 75%` (capped at `max-width: 1600px` so it doesn't run unbounded on ultra-wide monitors) - on a typical desktop viewport this is now the ~75%-of-page width asked for, versus the old fixed 960px column that left large gutters on anything wider than a laptop screen. `.landing-faq` dropped its separate, narrower `max-width: 68ch` (~68 characters, meaningfully narrower than the 960px section it sat inside) in favor of `width: 100%` so it now fills the same width as Open Positions instead of looking narrower next to it. Confirmed via `getBoundingClientRect()` that `.landing-faq`'s width is identical before and after expanding an accordion item (1272.8px both times in testing) - width was already layout-stable since these are block-level elements where only height changes on toggle, but this directly verifies the "fixed even if it's clicked" requirement rather than assuming it. Both sections fall back to `width: 100%` under the existing `@media (max-width: 720px)` breakpoint, since 75% of a phone-width viewport would be uncomfortably narrow once the section's own padding is subtracted.
- **Landing page: Open Positions cards now slide open/closed smoothly instead of popping in/out instantly**, per client request ("put sliding down animation when Open position is clicked, make it smooth"). The body content was previously conditionally rendered (`{expanded && <div>...}`), so it mounted/unmounted with no transition at all. It's now always rendered, wrapped in a new `.landing-position-collapse` div that toggles a `--open` class; the CSS animates `grid-template-rows` between `0fr` (collapsed) and `1fr` (open) over `0.3s ease` - the standard CSS-only accordion technique, since `height: auto` itself isn't animatable and this avoids needing JS to measure `scrollHeight` per card. `.landing-position-body` (the grid item inside) gained `overflow: hidden` and `min-height: 0` (grid items default to `min-height: auto`, which would otherwise stop the row from ever collapsing below its content's natural height). Respects `prefers-reduced-motion: reduce` (transition disabled). Verified in-browser: a screenshot taken mid-click shows the card genuinely mid-slide (description text visible but cut off partway through), confirming the animation actually runs rather than just working in the fully-open/closed end states.
- **Evaluate Applicants: "Details"/"Sift"/"Enter PQE Score"/"Evaluate Applicant"/"Schedule Oath-Taking" now open in a modal instead of expanding an inline table row**, per client request ("lets make it a modal instead of sliding down the information"). `EvaluationRow`'s `expanded` boolean (which toggled an extra `<tr><td colSpan={9}>` beneath the row) is now `showDetailsModal`, and that same content - the sifted/scheduled/rejected/compliance/oath-taking/hired status cards, the panel-score list, and whichever action form applies (sift decision, PQE score, schedule evaluation, schedule oath-taking) - renders unchanged inside a `wide` `Modal` (the same shared component `ApplicantDocumentsModal`/`ComplianceReviewModal` already use, titled `"<action> — <applicant name>"`, e.g. `"Sift — Gilbert Franz Anthony Ormeneta"`). The row's toggle button no longer doubles as a close control (previously read "Cancel" when expanded, since clicking it again collapsed the row in place) - closing is now the modal's own Escape/click-outside/× /footer "Close", consistent with every other modal in the app, so the button always shows its contextual action label (`detailsButtonLabel()`, renamed from `toggleLabel()` with the now-inapplicable "Cancel" branch removed). Every `setExpanded(false)`-on-success call in the four submit handlers (sift, PQE score, schedule evaluation, schedule oath-taking) now closes the modal the same way. Verified in-browser as `admin@dilg.gov.ph` against real seeded applications: "Sift" opens a modal titled "Sift — Gilbert Franz Anthony Ormeneta" with the decision form; "Details" on a fully-progressed (sifted → scheduled → rejected) application correctly shows all three status cards stacked in the modal body exactly as they previously rendered inline.
- **Full-system audit, per client request** ("Do not stop after finding the first error... audit → identify → reproduce → fix → test → regression test → report"). Static checks (`tsc`/`eslint`/`vitest`) across both workspaces, a route-by-route RBAC/auth review, a module-by-module business-logic read of every backend service, and live in-browser testing as all three roles (admin via the seed account; two throwaway `PANEL`/`APPLICANT` accounts created for the audit and deleted afterward). Four real issues found and fixed:
  - **Broken access control: `PANEL` could read any application's Compliance to Requirements checklist**, not just applicants they're assigned to interview. `ApplicationsService.listComplianceItems()` (`GET /applications/:id/compliance-items`, no route-level `requireRole` by design - an `APPLICANT` may read their own, an `ADMIN` any) only ever special-cased the `APPLICANT` branch; any other authenticated role fell through with zero scoping, unlike every other panel-facing endpoint (e.g. `DocumentsService.listForApplicant()`'s PDS-only + assignment-checked scoping for `PANEL`). Reproduced live: created a throwaway `PANEL` account assigned only to one applicant's interview board, then called the endpoint directly with its own token against a *different*, unrelated applicant's `HIRED` application - got back the full checklist (requirement name, `VERIFIED` status, the reviewing admin's user id, and the proof document's metadata) with no relationship between the two whatsoever. Fixed by forbidding any non-`APPLICANT`, non-`ADMIN` role outright (`PANEL` has no legitimate reason to see this stage at all - it's never called from `frontend/src/features/panel`). Re-verified the same exploit request now 403s, and that `APPLICANT`-owner and `ADMIN` access are both unaffected.
  - **Info disclosure: the server's absolute filesystem path (`Document.filePath`) was returned in every document-related API response** - discovered while reproducing the bug above (the exploited response's nested document included `C:\Users\Lenovo\Desktop\DILG_hiring\backend\uploads\...`). Not limited to that one endpoint: `DocumentsRepository.findByApplicant()`/`findById()` return the full, unfiltered `Document` row, and `DocumentsService.listMine()`/`listForApplicant()`/`upload()` all passed it straight through to the client - an applicant's own `GET /me/documents`, every upload response, and admin's "View Documents" modal all leaked it too. Not directly exploitable on its own (the file-serving route resolves files by DB-driven `filePath` looked up server-side by opaque id, never a client-supplied path) but a real CWE-200 gap with no legitimate reason to exist. Fixed with a new `PublicDocument`/`toPublicDocument()` (`documents.service.ts`) applied at every point a `Document` reaches an HTTP response, including the nested `documents[]` on compliance-item responses. Verified live: `GET /applicants/:id/documents` and the compliance-items response no longer contain `filePath`, while the actual "View"/"Download" file-serving endpoint (unaffected, never used the JSON field) still 200s.
  - **CompAss ranking didn't handle ties** - `PanelEvaluationsService.tabulation()` and the admin `ApplicantScoresModal`'s `rankRows()` both sorted descending and assigned `rank = 1, 2, 3...` with a plain running counter, so two applications with the exact same average score got different, array-order-dependent ranks instead of sharing one - a real fairness/correctness bug for a ranking that feeds actual hiring decisions, not cosmetic. Fixed both to standard competition ranking (1224...: tied rows share a rank, the next distinct value's rank is its 1-based sorted position). The backend fix was extracted into a standalone exported `assignCompetitionRanks()` and the frontend's `rankRows()` was exported, specifically so both could get real regression tests (see below) rather than only being exercised indirectly through a full page load.
  - **(Low-priority, self-inflicted-only) An admin could demote their own role with no guard**, unlike `UsersService.remove()`'s existing "cannot delete your own account" check for the same underlying risk (locking every admin out with no in-app recovery). Added the equivalent guard to `update()`: blocks an actual role *change* on your own account, not a same-value no-op or any other field. Verified live: self role-change 400s ("You cannot change your own role"), while self name-change and a same-role no-op PATCH both still 200.
  - Also discovered and fixed along the way: **`backend/` had no `vitest.config.ts`**, so the `@/*` path alias used throughout the entire backend (`tsconfig.json`'s `paths`, rewritten at build time by `tsc-alias`) was never resolvable by the test runner - meaning no backend test could ever have run, on any file, even before this audit. There were zero test files in either workspace despite `vitest` being a configured dependency in both `package.json`s; added a `resolve.alias` matching `tsconfig.json` and wrote the first real backend and frontend unit tests (`panel-evaluations.service.test.ts`, `ApplicantScoresModal.test.ts` - 5 and 4 cases respectively, covering plain ranking, top/middle ties, three-way ties, and null/unscored rows) to regression-test the ranking fix above and prove the test infrastructure now actually works. Full remaining test-coverage buildout is future work, not attempted here.
  - Everything above was re-verified after fixing: full `tsc --noEmit`/`eslint`/`vitest run` clean (0 errors) on both workspaces, a full click-through of all 9 admin sections plus the Evaluate Applicants details modal and Applicant Scores modal with zero console/network errors, and the two throwaway audit accounts deleted with no orphaned references left behind.
- **Full-system audit, round 2** - same client request repeated ("Do not stop after finding the first error... continue auditing until the entire system has been checked"), continued into areas round 1 hadn't reached yet: Excel/CSV formula-injection review, an email-template HTML-injection review, every remaining unreviewed backend module (`positions`, `audit-logs`, `dashboard`, `examScoreParser`), a live end-to-end applicant run (registration through all 5 steps, document uploads, job application, and withdrawal - round 1's live testing only reached step 2), and the frontend pages/components round 1 hadn't read yet. One new real bug found and fixed, plus a second lower-severity one, plus two things actively verified as *not* bugs:
  - **HTML injection in every outbound email** - `applicationEmailTemplates.ts`/`authEmailTemplates.ts` interpolate `applicantName` (fully free-text per `applicants.dto.ts`'s `z.string().min(1).max(100)` - no character restriction, confirmed by reading the schema) and admin-authored `jobTitle`/`venue`/`attire`/`notes`/`remarks`/`requirementNames` straight into hand-built HTML template literals with zero escaping - unlike the frontend, there's no React auto-escaping in an email string. Reproduced live: registered a throwaway applicant with the name `<img src=x onerror=alert(1)>Juan` and confirmed via the running app that it renders as inert literal text everywhere in the React UI (My Applications, the stage tracker - no `alert()` ever fired, confirming the frontend itself is safe) - the injection is specifically in the backend-generated email HTML, which the frontend never sees or re-renders. Blast radius is self-limited today (every affected email goes only to the applicant's own address, not a shared/admin inbox), but is a real CWE-79 phishing-enablement vector under the system's own sending identity via any HTML-rendering webmail client, and would become a full stored XSS the moment any of these templates is ever reused for a multi-recipient notification. Fixed with a new dependency-free `escapeHtml()` (`backend/src/shared/utils/escapeHtml.ts`) applied to every interpolated value in both template files, including the two `regretEmail()` call sites in `applications.service.ts` that build their own `reason` HTML fragment with an embedded job title. New tests: `escapeHtml.test.ts` (4 cases) and `applicationEmailTemplates.test.ts` (4 cases, asserting a malicious name/venue/attire/notes/requirement-name never survives into the rendered `html` while the template's own structural tags still do).
  - **(Lower severity) `PositionsService` never validated that `panelUserIds` actually belong to `PANEL`-role users**, unlike `PanelAssignmentsService.create()`/`bulkCreate()`'s existing check for the exact same data. Not reachable through the admin UI (`PositionsPage`'s picker already only fetches/lists `PANEL` users), so this only mattered for a direct API call - but a bad id there would silently produce a `PanelAssignment` nobody could ever act on once a job posting auto-assigns a Position's panel (the panel-evaluations routes gate on the *caller's own* role, not the assignment row), quietly shrinking the effective interview board with no admin-visible error. Fixed by adding the same `usersRepository.findByIds()` + role check to `create()`/`update()`, mirroring `PanelAssignmentsService`'s pattern exactly (`PositionsService` gained a `UsersRepository` constructor dependency, wired in `container.ts`). Verified live as admin: assigning an `APPLICANT`-role user's id now 400s ("… does not have the Panel role"), assigning a real `PANEL` user's id still 201s.
  - **Verified NOT a bug (Excel formula injection):** `exportPendingPqeScores()` writes applicant names and job titles into an `.xlsx` via `exceljs`. Rather than assume, generated a real test workbook with a formula-injection-style name (`=1+1+cmd|"..."`) and inspected the raw OOXML - `exceljs` writes it as a shared-string-typed cell (`t="s"`), not a formula cell, so Excel renders it as literal text on open. The classic CSV-injection heuristic only applies to untyped CSV import, not properly-typed XLSX cells.
  - **Verified NOT a bug (exam-score-import formula/crash risk):** `examScoreParser.ts`'s `xlsx.load()` doesn't evaluate formulas (`exceljs` isn't a calculation engine) - a formula cell's `.value` is an object, which `String(...)`s to `"[object Object]"` and `Number(...)`s to `NaN`, so it's silently skipped by the parser's existing `!Number.isFinite(score)` guard rather than causing a crash or unexpected value.
  - **Verified NOT a bug (client-spoofed upload `mimetype` → stored-XSS-via-Content-Type):** a malicious applicant could set a file's declared `mimetype` to an allowed type (e.g. `application/pdf`) while uploading actual HTML/JS content, which the server would later serve back verbatim as the `Content-Type` header on view. Confirmed live via response headers that `helmet()`'s defaults are active on the running server (`X-Content-Type-Options: nosniff` + a `Content-Security-Policy: default-src 'self'` with no `unsafe-inline`), which closes the MIME-sniffing angle even when declared and actual content type disagree.
  - Re-verified after fixing: full `tsc --noEmit`/`eslint`/`vitest run` clean (0 errors, 13/13 tests passing including the 5 new round-2 tests) on both workspaces, a full admin click-through with zero console/network errors, and the throwaway applicant account (and test position) deleted afterward with no orphaned data.
- **PQE-score lock on panel scoring, per client request** ("Panel should not be able to score applicant if there's still no PQE exam score"). `applications.scheduleInterview()` already refused to move an application into `FOR_INTERVIEW` without a recorded `examinationScore`, so this couldn't happen through the normal flow - added an explicit `examinationScore === null` guard directly in `PanelEvaluationsService.submit()` (`PATCH /api/panel-evaluations/:applicationId` now 400s "Cannot score this applicant until a PQE exam score has been recorded") as a second, independent gate so panel scoring never silently depends on that earlier check alone. Frontend: `GET /my-queue`/`InterviewQueueApplication` now expose `examinationScore`; `MyInterviewsPage`'s queue table gained a "PQE score" column, and `InterviewRow`'s Score/Update scores button is disabled (with an inline explanation row) whenever it's `null`. Verified live: created a throwaway `PANEL` account, assigned it to an existing `FOR_INTERVIEW` applicant with a recorded PQE score of 100, confirmed the queue shows the score and the Score button is enabled, and submitted a real evaluation end-to-end (status flipped to "Scored") to confirm no regression - then deleted the throwaway account (cascades its `PanelAssignment`/`PanelEvaluation` rows, per `docs/database.md`).
- **Search on the History of Logs page, per client request** ("easier to find log that has been long time ago"). Unlike `UsersManagementPage`'s client-side email filter (which fetches every user unbounded), audit logs are always capped at `limit` (default 200, max 500) — so a client-side filter over the already-fetched page couldn't find an older match sitting past that cutoff, exactly the "long time ago" case being asked for. Instead added a `search` query param (`listAuditLogsQuerySchema`) that `AuditLogsRepository.findMany()` applies in the Prisma `WHERE` clause (case-insensitive `contains` OR across `action`, `details`, and the actor's email) *before* `take: limit` runs, so a match beyond the most-recent-N window still surfaces. Frontend: `AuditLogsPage` gained a "Search" text field (reusing the `.filters-row` layout `UsersManagementPage`/`JobManagementPage` already established) alongside the existing entity-type dropdown, debounced 300ms so typing doesn't fire a request per keystroke; the empty state now distinguishes "No log entries yet." from "No log entries match your search/filter." the same way `JobManagementPage` already does. Verified live as admin: searching an assigned/deleted test panelist's email surfaced exactly the 3 log rows mentioning it (User Created/Panel Assigned/User Deleted) while correctly excluding an unrelated row from the same time window with no matching text; a nonsense search term correctly showed the "no match" empty state; clearing the field correctly restored the full list.
- **Admin-initiated password reset, closing the top item of `docs/project-memory.md`'s Future Work list** (applicant self-service forgot-password existed since 2026-08-12, but an `ADMIN`/`PANEL` account locked out of their own password had no in-app recovery path — an operator would've had to touch `passwordHash` directly in the database). New `POST /api/users/:id/reset-password` (`UsersService.resetPassword()`, `ADMIN`-only) issues a fresh temporary password via the same `generateTemporaryPassword()`/`hashPassword()`/`temporaryPasswordEmail()` the forgot-password flow already uses, sets `mustChangePassword`, and audit-logs it under the existing `USER_TEMPORARY_PASSWORD_ISSUED` action with `actorUserId` set to the admin (not the target user, unlike the self-service flow) so the trail shows who reset it. Left unrestricted by role — works for `APPLICANT` accounts too, since there's no reason an admin helping a locked-out applicant should be forced through the email-based self-service flow instead. `UsersRepository` gained `findByIdForPasswordReset()` (email + name + applicant first/last name, for the email greeting) and `setTemporaryPassword()`, mirroring `AuthRepository`'s existing methods. Frontend: `UsersManagementPage` gained a "Reset Password" button per row opening a non-destructive (`danger={false}`) `ConfirmDialog`. Verified live as admin: created a throwaway `PANEL` account, reset its password twice (both attempts confirmed via the History of Logs audit trail — the devtunnel serving this dev environment dropped the HTTP response back to the browser on both attempts, showing as a transient network error client-side, but the backend itself completed and logged both resets correctly, confirming the feature works independent of that unrelated tunnel flakiness), then deleted the throwaway account.
- **Users Management showed no name for applicant accounts, and dropped the Email column per client request.** `GET /api/users`'s `publicUserSelect` only ever selected `User.name` (`ADMIN`/`PANEL`-only, set via this page's own form) and never joined in the `Applicant` profile's `firstName`/`lastName`, so every `APPLICANT` row's Name cell rendered the `—` empty-state regardless of whether the applicant had actually registered. Fixed by adding `applicant: { select: { firstName, lastName } }` to `publicUserSelect`/`PublicUser` (`users.repository.ts`) - the same join `findByIdForPasswordReset()` already used for its email-greeting fallback, now available to every `listUsers()` caller too. Frontend: new `getDisplayName()` helper (`UsersManagementPage.tsx`) - `User.name` first, else the applicant's full name, else email as a last resort - replaces the table's separate Email/Name columns with one Name column, and also feeds the row-action toasts/confirm dialogs (previously always `.email`) so they read consistently with what's now shown. The client-side search filter (still an unbounded fetch-then-filter, same tradeoff noted in the History of Logs entry above) now matches against display name or email, with the placeholder updated to say so - email remains a fully valid search term even though it's no longer a visible column. No backend/database field was removed; `User.email` is still returned by the API and still shown in the "Reset Password" confirmation (naming the actual email address the temporary password gets sent to). Verified live as admin: registered/seeded applicant accounts now show their real name (e.g. "Juan Dela Torre") instead of "—", the table has no Email column, and search-by-email still narrows results correctly.
- **Dropped the Email column from Evaluate Applicants, per client request, so the row's action buttons ("View Documents", "Manage Compliance", "Move to Compliance", "Not Selected"/"Disqualify", "Sift"/"Enter PQE Score"/"Evaluate Applicant"/"Schedule Oath-Taking"/"Details") no longer force horizontal scrolling on the table.** `EvaluationRow.tsx`'s `<td>{application.applicant.user.email}</td>` and its matching `<th>Email</th>` (`EvaluateApplicantsPage.tsx`) were removed; the table's `colSpan` on its empty-state rows dropped from 9 to 8 to match. No API/database change - `matchesSearch()` still filters by `application.applicant.user.email` under the hood, so the existing "Search by applicant name or email..." box keeps working even though email is no longer a visible column, the same tradeoff as the Users Management fix above. Verified live as admin: the table now fits without a horizontal scrollbar and every row's action buttons are fully visible.
- **Structured qualification minimums for Sifting, per client request** ("how can the system determine that applicant has this Education, Eligibility, Training and experience" - the four `qualification*` fields on a job posting were pure free text with no applicant-side structured counterpart to check against, aside from eligibility). New optional `JobPosting.minEducationLevel`/`minYearsExperience`/`minTrainingHours` (all `null` by default - no automatic check unless an admin sets one) and new required `Applicant.educationLevel` (an ordered `EducationLevel` enum, `ELEMENTARY`…`DOCTORATE`, collected on the Demographic Profile step) plus `Applicant.yearsOfExperience` (a single self-reported number, deliberately *not* a revived `WorkExperience` entries list - see [decisions.md](./decisions.md) for why that table stays unused). `EvaluateApplicantsPage`'s Sift modal now opens on a Qualification Standards card showing each free-text QS line next to a "Meets"/"Below minimum"/"No automatic check" badge, computed entirely client-side (`frontend/src/shared/utils/qualificationMatch.ts`) so it's always correct after any row-updating action, never a server-persisted field that could go stale. The free-text QS fields remain the authoritative wording throughout - the badge is explicitly framed as a hint, and the Sifting decision itself is still a human `QUALIFIED`/`NOT_QUALIFIED` call, not a system-enforced gate (unlike `requiredEligibilityTypes`, which still hard-blocks *submission*, a different point in the pipeline). Backend: `applications.repository.ts`'s `applicationWithApplicantInclude` now also joins `applicant.ldInterventions` and expands `jobPosting.requiredEligibilities` into `requiredEligibilityTypes` (closing a latent gap where `GET /api/applications` never actually returned that field despite the frontend type claiming it did) via a new `toApplicationWithApplicant()` reshape helper applied across every repository method. `JobManagementPage`'s form gained three new optional inputs (a select + two number inputs, all with "no automatic check" blank states) and `DemographicProfileForm` gained "Highest educational attainment" (select) and "Total years of relevant work experience" (number 0-60). Verified live end-to-end via a throwaway applicant/posting/application (API-seeded, then viewed in the browser): Education showed "Below minimum" (Bachelor's vs. a Master's requirement), Experience and Eligibility both showed "Meets", and Training showed "No automatic check" (posting left that minimum unset) - all four badges rendering with the correct color/label; both new forms pre-filled correctly on edit. Migration `20260819045519_add_structured_qualification_minimums`; `npm run typecheck`/`lint`/`test` clean on both workspaces.
- **Fix: applications could get stuck at `FOR_COMPLIANCE` forever with an empty checklist, per client report ("I already applied and the application is hanging because there's no compliance in the list").** `moveToCompliance()` only ever snapshots the *currently*-active `ComplianceRequirement` catalog - if that catalog was empty (or had nothing active) the moment an applicant moved to Compliance, `ApplicationComplianceItem` never got any rows, and `EvaluationRow`'s `allComplianceVerified` (`complianceItems.length > 0 && ...`) could then never become true, permanently blocking "Schedule Oath-Taking" with no way for the admin to recover from inside the app. Two changes close this: (1) new `POST /api/applications/:id/compliance-items` (`AddComplianceItemDto`, `{ requirementId }`, `ADMIN`-only) lets an admin manually attach one more requirement from the catalog to a single already-`FOR_COMPLIANCE` application (400 if not `FOR_COMPLIANCE`, 404 if the requirement doesn't exist, 409 if already on the checklist) - `ComplianceReviewModal` gained an "Add a requirement to this applicant's checklist" picker (sourced from `GET /compliance-requirements`, filtered to what's not already listed) above the table; (2) `reviewComplianceItem()` no longer 400s when no `COMPLIANCE_PROOF` document has been uploaded - the admin can now select a requirement and mark it Verified/Rejected on their own judgment (the same discretion `sift()`'s decision already carries), instead of the system's automatic snapshot being the only path to a reviewable item. `ComplianceReviewModal`'s Verify/Reject buttons and remarks field are shown unconditionally instead of only once a proof document exists. New audit action `APPLICATION_COMPLIANCE_ITEM_ADDED`. No schema change (existing `(applicationId, requirementId)` unique constraint already supports this). See `docs/project-memory.md`'s Known Limitations entry (updated) for the full before/after.
- **Same-day follow-up, per client request: "The compliance should only be verified once the Applicant upload the required document needed and has been approved by the Admin. Add option for compliance hardcopy(physical) or softcopy(online)."** The proof-optional loosening from the fix directly above was reinstated for the normal case and replaced with a more precise rule: `VERIFIED` now again requires an uploaded `COMPLIANCE_PROOF` document — *unless* the item is explicitly marked Hardcopy, since a physically-submitted requirement has no online proof to upload in the first place. New `ApplicationComplianceItem.submissionType` column (`ComplianceSubmissionType`: `SOFTCOPY` default, `HARDCOPY`; migration `20260819115725_add_compliance_submission_type`), set per item by an admin — never the applicant — via new `PATCH /api/applications/:id/compliance-items/:itemId/submission-type` (`ApplicationsService.setComplianceItemSubmissionType()`, new audit action `APPLICATION_COMPLIANCE_ITEM_SUBMISSION_TYPE_SET`); `POST /api/applications/:id/compliance-items` also gained an optional `submissionType` so an admin can set it at add time. `reviewComplianceItem()`'s `VERIFIED` gate now checks `submissionType === "SOFTCOPY" && documents.length === 0` (400, naming the item and suggesting Hardcopy if it applies) — a `HARDCOPY` item skips the check entirely, since the admin's own `VERIFIED` there *is* the record that the physical copy was received and approved. `REJECTED` stays unconditional either way. `ComplianceReviewModal` gained a per-row "Submission" `<select>` (Softcopy/Hardcopy, editable any time) and a matching selector next to "Add a requirement" for new items; the Proof column reads "Hardcopy - no online copy expected" instead of "Not submitted yet" for Hardcopy items with none; the Verify button is `disabled` with an explanatory `title` for a Softcopy item still missing proof. Verified live as admin end-to-end: added a Softcopy test item with no proof and confirmed Verify was disabled (and a direct `PATCH` attempt 400'd); flipped it to Hardcopy and confirmed Verify then succeeded (confirmed via direct `PATCH` too); cleaned up all test compliance items/requirements afterward, leaving the real applicant's checklist exactly as the previous fix had left it. See `docs/decisions.md`'s 2026-08-19 "proof-optional loosening reinstated" entry for the full rationale.
- **Fix: the "Verify" button in Manage Compliance looked broken after an applicant uploaded proof, per client report ("I uploaded using the applicant account and verify button is not working").** Root-caused live: the backend call was actually succeeding every time (confirmed via the audit trail and a direct API round-trip - each click updated `reviewedAt`/`reviewedByUserId` and returned 200), but `ComplianceReviewModal`'s `handleReview()`/`handleAddRequirement()`/`handleSubmissionTypeChange()` were the only admin-mutating actions in the entire app that never called `toast.success()` on success, unlike every other action in `EvaluationRow.tsx` and elsewhere. Combined with an item that was already `VERIFIED` from earlier testing, clicking Verify again produced a real, successful, but *silent* no-visible-change request - indistinguishable from the button doing nothing. All three actions now show a confirmation toast (e.g. `Marked "Required1" Verified for Gilbert Ormeneta.`) on success, matching the established pattern. Verified live: Reject → toast + badge flips to REJECTED; Verify → toast + badge flips back to VERIFIED.
- **Evaluation Criteria renamed to Categories/Criteria, with scoring moved down to the individual criterion/question, per client request** ("lets rename it to categories and the questions to criteria/question, each criteria/question should have point on its own and will be average from the scores of panels... even there's one missing panel score we can still have the score"). Previously a panelist scored one number per top-level `EvaluationCriterion` ("category"); its guiding `EvaluationCriterionQuestion[]` were purely descriptive text with no point value. Now: `Category` (renamed from `EvaluationCriterion`, table `categories`) is a non-scored grouping with **no stored `maxScore`** - its point value is the sum of its own active `Criterion` rows' `maxScore`, computed at read time so the two can't drift apart; `Criterion` (renamed from `EvaluationCriterionQuestion`, table `criteria`, `text` → `name`) gains real `maxScore`/`isActive` fields and is what `PanelScore.criterionId` now points at - a panelist scores every criterion/question individually, and a category's reported value is that sum, per panelist, averaged across however many panelists actually submitted (already-existing "missing panelist just shrinks the denominator" tabulation behavior, now meaningful at the sub-category level too). Because a `Criterion` can carry real historical scores (the old question text never could), `CategoriesRepository.update()` replaced the old "delete every question, recreate from scratch" convention with a diff: entries with an `id` update in place, new ones are created, and anything removed from the array is blocked (409) if it already has a recorded score - deactivate it instead, same shape as every other delete-guard in this codebase. Backend module/route renamed `evaluation-criteria`/`/api/evaluation-criteria` → `categories`/`/api/categories`; audit actions `CRITERION_*` → `CATEGORY_*`; frontend `EvaluationCriteriaPage` (`/admin/criteria`) → `CategoriesPage` (`/admin/categories`), sidebar label → "Categories"; panel scoring (`InterviewRow`) now renders one score box per criterion grouped under its category heading instead of one box per category; `CriteriaReferencePanel`/`ScoreSummaryPanel`/`ApplicantScoresModal` updated to match (the latter's per-column view is now per-category subtotal, not per-criterion). **Data migration:** the one real scored record in this environment ("Extemporanious Speaking", 25 pts, 1 submitted score of 25) had no per-question point breakdown to decompose honestly, so rather than fabricate one, the migration collapsed it to a single fallback criterion carrying the category's full point value and repointed the existing score to it - verified after migration the score is still exactly 25/25, now attached to the criterion instead of the category. Applied as a hand-written `RENAME TABLE`/`CHANGE COLUMN` migration (`20260819123048_rename_criteria_to_categories`) preceded by a raw-SQL pre-migration data-fixup script, rather than Prisma's default destructive drop-and-recreate diff, so no data was lost. Verified live end-to-end as both admin and panel: edited the migrated category, added a second criterion (diff-based update correctly kept the scored one and added the new one, category total recalculated 25→40), confirmed attempting to remove the *scored* criterion was blocked with a clear 409 error, and confirmed the panel-facing rubric reference panel correctly renders "Category (0-40)" with both criteria nested under it with their own point ranges; cleaned up the test criterion afterward, restoring the original 25-pt state. See `docs/decisions.md`'s 2026-08-19 "Evaluation Criteria renamed to Categories/Criteria" entry for the full rationale and trade-offs.
- **Same-day follow-up, per client request: "If I clicked Category I should have an input how much percent is this category for the overall evaluation. Example I put 25% in Battery test even I have many criteria max point should still be 25% of the overall evaluation."** The category rework above still let a category's raw criteria total *be* its effective weight, coupling the overall score to how a category happened to be broken down internally - a category split into 10 small criteria could silently outweigh one with a single criterion, even at "equal" intended importance. Fixed with a real, admin-set `Category.weightPercent` column (`Int`, 1-100, required - migration `20260819130950_add_category_weight_percent`), entered on a new "Weight (% of overall evaluation)" field on the Add/Edit Category form. It's independent of the criteria list: a category worth 25% stays worth 25% whether it has 1 criterion or 20, worth 10 raw points or 1000. New `weightedCategoryScore(rawSubtotal, rawMax, weightPercent)`/`weightedTotalScore()` helpers (`panel-evaluations.service.ts`, both exported and unit-tested with the client's exact scenario reproduced) normalize a panelist's raw per-category subtotal against the category's raw max (the existing derived `maxScore`) and scale it to `weightPercent` — this is now what both `GET /panel-evaluations/tabulation/:jobPostingId` (CompAss ranking) and `GET /panel-evaluations/applicant-scores` report, replacing the flat raw-sum totals from the rename above. `CategoriesPage` shows a live "Total weight across active categories: N%" hint (in the list and, projected, inside the add/edit form) — a soft warning if it's not 100%, never a blocking error. Panel-facing UI (`InterviewRow`, `CriteriaReferencePanel`) now labels each category heading `"<name> — <weightPercent>% of overall evaluation (raw scoring 0-<maxScore>)"` so panelists see the weighting context even though they still fill in raw per-criterion scores; `ScoreSummaryPanel`'s "My total score" is now the panelist's weighted total too (new shared `frontend/src/shared/utils/weightedScore.ts`, mirroring the backend math so a panelist's self-view matches what actually counts). **Migration:** additive column, add-nullable → backfill → enforce-NOT-NULL; the two categories that existed at the time were both backfilled to 100 (no other information to infer a real split from). Verified live end-to-end as admin: both categories showed 100%/100% with the new "200%" mismatch warning; edited "Battery Test" (4 criteria, 40 raw points) to 25% and "Extemporanious Speaking" (1 criterion, 25 raw points) to 75% through the actual UI, confirmed the hint cleared to a clean "100%", confirmed `GET /panel-evaluations/applicant-scores` now returns `weightPercent` (not raw `maxScore`) per category, and confirmed as the panelist that the rubric reference panel correctly reads "Battery Test — 25% of overall evaluation (raw scoring 0-40)" alongside its 4 nested criteria - exactly demonstrating that a 4-criterion, 40-point category is still capped at 25%. 6 new backend unit tests (`weightedCategoryScore`/`weightedTotalScore`) added, including one reproducing the client's literal example (two categories at the same 25% weight, one with 1×10-point criterion and one with 4×25-point criteria, both scored to max — both correctly total exactly 25, not 10 vs. 100). See `docs/decisions.md`'s 2026-08-19 "Category.weightPercent" entry for the full rationale and trade-offs.
- **Removed the "Total weight across active categories" hint from the Categories list, per client request.** The Add/Edit Category form's own live "Active categories would total N% with this value" hint (shown while typing a weight) stays - only the redundant list-level summary below the table was removed.
- **Dashboard's "Applications by status" changed from a horizontal bar chart to an inline-SVG pie chart, per client request** ("can we make it a pie chart instead"). New `PieChart` (`DashboardPage.tsx`) renders one slice per status with at least one application (empty statuses are skipped rather than drawn as zero-width slices), direct-labels a slice's percentage only past an 8% share to avoid label collisions, and always shows a legend (swatch, label, exact count) below the chart - reuses the same status colors already used for `.badge`s and the existing bar chart. Handles the single-100%-slice edge case (an SVG arc can't have identical start/end points) by capping the sweep just under a full turn. **Fix found while verifying this live:** the real `HIRED` application in this environment reported `applications.total: 0` and never appeared as a slice - `dashboard.dto.ts`'s hand-maintained `APPLICATION_STATUSES` list had never been updated when the Compliance to Requirements/Oath-Taking phases added `FOR_COMPLIANCE`/`NOT_SELECTED`/`DISQUALIFIED`/`FOR_OATH_TAKING`/`HIRED` to the `ApplicationStatus` enum, so `DashboardService.tally()` silently excluded every application in one of those 5 statuses from both the total and the breakdown - not miscounted, entirely absent, no error anywhere. Fixed by adding all 11 statuses to the list, and added new `dashboard.dto.test.ts` comparing `APPLICATION_STATUSES`/`JOB_POSTING_STATUSES`/`USER_ROLES` against `Object.values()` of their actual Prisma-generated enums, so the same class of drift fails a test instead of silently under-reporting again. Verified live as admin: the dashboard's Applications stat tile went from `0` to the correct `1`, and the pie chart now shows a single "Hired" slice at 100% with the matching legend entry. See `docs/decisions.md`'s 2026-08-19 "Dashboard 'Applications by status' switched to a pie chart" entry for the full rationale.
- **Same-day follow-up, per client request: "Also the Top Job posting by Application."** Converted the dashboard's second chart, "Top job postings by applications," from the same horizontal `BarChart` to the shared `PieChart` component - which then made `BarChart` fully dead code (both dashboard charts now use `PieChart`), so it and its now-unused CSS (`.bar-chart`/`.bar-row`/`.bar-row-label`/`.bar-track`/`.bar-fill`/`.bar-value`) were deleted rather than left unreferenced. `PieChart`'s shared `BarRow` prop type was renamed `ChartRow` (no longer bar-specific), and since it's now rendered under two different headings it gained a required `ariaLabel` prop instead of a hardcoded `aria-label="Applications by status"`. Unlike the status chart, a job posting has no existing semantic color (no `.badge` token per posting), so this chart introduces a new fixed 5-hue categorical palette (`CATEGORICAL_CHART_COLORS` in `DashboardPage.tsx`, sourced from the `dataviz` skill's validated categorical set) assigned by array index - sized to 5 because the backend's `topJobPostings` is already capped at `TOP_JOB_POSTINGS_LIMIT = 5` (`dashboard.service.ts`), so the palette never needs to cycle. The always-shown legend (swatch, title, exact count) is what keeps a posting identifiable even though the palette's guaranteed colorblind-safe separation only covers 3 adjacent slices, not all 5 - no posting is ever identified by color alone. Verified live as admin: both dashboard pie charts render correctly (single 100%-slice "Hired"/"Administrative Assistant III" cases in this environment, since only one application exists), `npm run typecheck`/`lint`/`test` all pass in `frontend`. See `docs/decisions.md`'s 2026-08-19 "Dashboard 'Top job postings by applications' switched to a pie chart" entry for the full rationale.
- **New "Group" admin page for Group Dynamics Evaluation, per client request** ("New page in Admin 'Group' in group page Admin can Group applicants this is for Group dynamics Evaluation. Same setup with Interview Panel page where we have checkbox and instead of Assign Panel button it will be Group and it will open a modal for the name of group and description."). Not part of `docs/rsp-domain-spec.md` - new scope. New `applicant-groups` module (`ApplicantGroup` 1:N `ApplicantGroupMember` 1:1 `Application`, migration `20260819230413_add_applicant_groups`) and `GroupsPage` (`/admin/groups`, "Group" in the sidebar next to "Interview Panel"), mirroring `PanelAssignmentsPage`'s selection UI (search/job-posting/publication filters, checkbox multi-select, bulk action bar) with the bulk action swapped: a "Group" button (disabled below 2 selections) opens a two-field modal (name, description) and creates the group directly via `POST /api/applicant-groups`, no panel-member-style picker. A "Group(s)" column on the applicant table shows existing memberships, the same role "Assigned Panel" plays on Interview Panel; a second table below lists every group (name, description, members, created date) with Edit (`PATCH`, renames/redescribes only) and Delete (cascade-deletes members, no usage guard) actions. Two deliberate departures from a literal Interview Panel copy: `ApplicantGroup` carries no `jobPostingId` (a group can mix applicants across postings, unlike `PanelAssignment` which is keyed to one posting's board), and **group membership is fixed at creation time** - there's no add/remove-member flow yet, only rename/redescribe; fixing a wrong roster today means delete-and-recreate. Every write is audit-logged (`APPLICANT_GROUP_CREATED/UPDATED/DELETED`). **Migration note:** written by hand rather than via `prisma migrate dev`, because that command's shadow-database replay fails on an earlier hand-written migration in this same folder that isn't idempotent from an empty database (a pre-existing quirk, unrelated to this change) - applied directly via `prisma db execute` + `prisma migrate resolve --applied` instead, producing the identical schema `migrate dev` would have. Verified end-to-end: created a temporary second applicant/application via a throwaway script, exercised create/list/update/delete through both the raw API and the live UI (toast confirmations, the Group(s) column, the Groups table, the audit trail all correct), then deleted all temporary data and scratch scripts, restoring the environment to its prior single-applicant state. `npm run typecheck`/`lint`/`test` pass in both workspaces. See `docs/decisions.md`'s 2026-08-19 "New 'Group' admin page for Group Dynamics Evaluation" entry for the full rationale and trade-offs.
- **Same-day follow-up, per client request: "Yes build also member-editing the add/remove member."** `PATCH /api/applicant-groups/:id` now accepts an optional `applicationIds` (min 2) alongside `name`/`description` - when given, it's the group's new full member list, diffed against what's on file (`ApplicantGroupsRepository.update()`, same add/remove-by-diff shape `CategoriesRepository.update()` uses for a category's criteria). No new picker UI: each group row's new "Members" action seeds the existing applicant table's checkbox selection with that group's current roster and switches the bulk action bar into an editing mode ("Editing members of '<name>'" + "Save Members"/"Cancel" in place of "Group"/"Clear selection") - the admin adjusts membership with the exact same checkboxes used to create a group, then "Save Members" persists the new selection. Verified live end-to-end: created a temporary 2-member group via the UI, used "Members" to swap one member for a third applicant (add one, remove one, in a single save), confirmed the toast, the Groups table's member list, and the applicant table's "Group(s)" column all updated correctly, then deleted all temporary test data. `npm run typecheck`/`lint`/`test` pass in both workspaces. See `docs/decisions.md`'s 2026-08-19 "Same-day follow-up: member editing added" entry.
- **Applicant top nav reordered to Job Postings, My Applications, My Profile, per client request** (was Job Postings, My Profile, My Applications). `frontend/src/shared/components/Layout.tsx` only - no route/component changes.
- **`apiClient`'s backend URL is now resolved at runtime instead of baked in at dev-server start, fixing an intermittent "login failed" bug.** Pointing `VITE_API_URL` at a devtunnels URL (to demo over a forwarded port) had forced *every* visitor - including plain `localhost:5173` access - through that same tunnel, so a tunnel hiccup surfaced as an unexplained login failure locally too. `apiClient.ts` now derives the backend URL from the page's own hostname: a devtunnels frontend host (`<id>-5173.<region>.devtunnels.ms`) is mapped to its matching backend tunnel (`<id>-4000...`) automatically, while every other host (`localhost`, a real domain, etc.) still uses `VITE_API_URL` as before. `frontend/.env`'s `VITE_API_URL` reverted to `http://localhost:4000/api`. Verified live: `localhost:5173` and the devtunnels URL both worked simultaneously off the same running dev servers, each hitting its own correct backend.
- **AWS free-tier test deployment scaffold** (`deploy/aws-ec2/`), prepared ahead of the user creating their AWS account: `README.md` (account creation → EC2 launch → security group → provisioning → verifying → tearing down, written for someone new to AWS), `setup.sh` (one-time: installs Node/nginx/MySQL, creates a dedicated non-login service user, clones the repo, creates the app's MySQL database/user, generates `JWT_SECRET`, installs the nginx site and systemd unit, runs the first deploy), `deploy.sh` (repeatable: pull, install, `prisma migrate deploy`, build, restart), `nginx.conf` (serves the built frontend, reverse-proxies `/api` to the backend on `127.0.0.1:4000` only - never exposed directly), `dilgr8rsp-backend.service` (systemd, runs as the dedicated service user, sandboxed to only writing `backend/uploads/`), and `.env.production.example` templates for both workspaces. Single EC2 instance (nginx + backend + MySQL together) rather than RDS/S3, matching local dev's shape and free tier's per-service hour limits - see `docs/decisions.md`'s 2026-08-20 entry for the full rationale and what to revisit before any real (non-test) deployment. New `backend`'s `prisma:deploy` script (`prisma migrate deploy`, the non-interactive production counterpart to the existing dev-only `prisma:migrate`). Not yet exercised against a real AWS instance (no account existed yet) - see `docs/project-memory.md`'s Outstanding Tasks.
- **Fix: `deploy/aws-ec2/setup.sh`/`deploy.sh` hit five separate real-world bugs on their first actual run against a fresh AWS account/instance**, each fixed live: (1) both scripts were committed as non-executable - Windows' `core.fileMode=false` had silently dropped a local `chmod +x` before the original commit; fixed the committed mode and `setup.sh` now also `chmod +x`s both scripts right after cloning so this can't regress regardless of the committer's git config. (2) `apt`/`needrestart` mid-install prompts looked like the script hanging - `DEBIAN_FRONTEND=noninteractive`/`NEEDRESTART_MODE=a` now force both fully automatic. (3) The public-IP lookup 401'd - EC2's instance metadata service now requires an IMDSv2 session token, not a plain GET; `setup.sh` requests one first. (4) MySQL was OOM-killed in a restart loop - a t2/t3.micro's ~1GB RAM with no swap wasn't enough for MySQL plus the `npm ci`/build step; `setup.sh` now creates a 2GB swap file. (5) **The significant one**: `prisma migrate deploy` failed with "Duplicate foreign key constraint name 'panel_scores_criterionId_fkey'" replaying the full migration history against a genuinely empty database - `20260819123048_rename_criteria_to_categories` (see the 2026-08-19 Categories/Criteria rework above) had only ever been correct on the one database it was hand-applied against; fixed the actual migration file (`DROP FOREIGN KEY IF EXISTS` before the colliding `ADD CONSTRAINT`) rather than adding another one-off workaround, and verified by replaying all 32 migrations against a throwaway fresh local database end-to-end. See `docs/decisions.md`'s 2026-08-20 "Fixed `20260819123048_rename_criteria_to_categories`'s root cause" entry for the full rationale.
- **Admin sidebar reordered by data dependency, per client request** ("Re-arrange our side nav pages based on the needed to have data first. Example: Position comes first with Job Management since Positions is needed to have a data first before adding a job."). `AdminShell.tsx`'s `NAV_ITEMS` (and the matching `<Route>` order in `App.tsx`, for readability only) changed from build-order to: Dashboard, Users Management, Positions, Job Management, Categories, Compliance Requirements, Interview Panel, Group, Evaluate Applicants, History of Logs. Traced each section's real prerequisite: Users Management moved up because Positions' default panel members and Interview Panel's board assignments both pick from existing `PANEL`-role Users; Positions moved before Job Management per the client's own example (a posting is optionally created from a Position); Categories/Compliance Requirements grouped as independent setup catalogs right after posting setup; Interview Panel/Group/Evaluate Applicants ordered by how much of the pipeline they depend on already existing (board assignment needs postings; grouping needs actual applications; the Evaluate Applicants pipeline hub needs essentially everything). UI-only change - no routes, endpoints, or component names touched. Verified live: sidebar renders in the new order, clicking each link still navigates to the correct page. See `docs/decisions.md`'s 2026-08-19 "Admin sidebar reordered by data dependency" entry for the full per-section reasoning.
- **User Manual (`docs/manual/DILGR8RSP-User-Manual.docx`, mirrored at `frontend/public/`) brought current** - it hadn't been touched since 2026-08-12 and had drifted behind every feature added since: Categories/weighted scoring (renamed from Evaluation Criteria), the new Group Dynamics Evaluation admin page, structured qualification minimums on a job posting, the automatic Meets/Below minimum/No automatic check Sifting hint, Compliance's new Both (softcopy + hardcopy) submission type, the Dashboard's switch to pie charts, and the two new Demographic Profile fields (highest educational attainment, years of experience). Edited via direct OOXML surgery on the existing `.docx` (no docx-authoring library available) rather than a from-scratch rebuild, to keep every existing section's original formatting/branding intact - see `docs/decisions.md`'s 2026-08-21 entry for why and how. Real screenshots throughout (captured live off `localhost:5173`, same convention the original manual used, all cropped to the same 1568×716 the rest of the document already used so no image had to be resized/distorted): three figures swapped in place (Dashboard, Categories, Demographic Profile - same slot, same figure number, just new image + updated caption/body text) and three new figures (Group, Job Management's new fields, Compliance's Both option) appended as new steps at the end of Part II so no existing figure had to be renumbered. Verified by re-extracting the rebuilt `.docx` and confirming balanced XML tags, a passing zip integrity test, and that `frontend`'s dev server serves the identical byte-for-byte file at `/DILGR8RSP-User-Manual.docx`.
- **New admin "Report Summary" page** (`/admin/report-summary`, sidebar between Evaluate Applicants and History of Logs), per client request: an admin needed to see interview scores combined across every panelist, by Category and by Criterion, without seeing any one panelist's individual score. Backend: `PanelEvaluationsService.applicantScoresOverview()` (already the source for the existing "Applicant Scores" modal's Category-level combining) extended to also compute a per-Criterion average - `ApplicantScoresOverview` gained a `criteria` column list and each row a `perCriterion` map (raw 0-maxScore average across submitted panelists, same "combined = average across panelists" convention already used everywhere else panel scores are aggregated in this app). Frontend: `ReportSummaryPage.tsx` - one row per scored application (Category columns + overall total, no panelist column anywhere), with a "Criteria" expand toggle per row revealing every Category's own Criteria and their combined scores nested underneath. Deliberately doesn't duplicate the CompAss tabulation view (Evaluate Applicants), which is the one place per-panelist detail is still shown, by design, to an admin who specifically needs it. Verified end-to-end: seeded a temporary PanelEvaluation with known per-criterion scores directly against the one real local application (temporarily flipped its status to QUALIFIED, since the underlying query only reads FOR_INTERVIEW/QUALIFIED/NOT_QUALIFIED applications), confirmed the weighted category math and raw criterion averages both matched hand-calculation exactly via the live API and the rendered page's expand view, then deleted the test evaluation and restored the application's original HIRED status.
- **Fix: `apiClient.ts`'s tunnel-URL detection crashed the entire frontend test suite.** `resolveApiUrl()` (added when fixing the devtunnels login-reliability bug) read `window.location` at module-evaluation time; vitest's default test environment has no `window` (it's Node, not jsdom), so any test file that transitively imported `apiClient.ts` failed to even collect, with `ReferenceError: window is not defined` - undetected until this session's own `npm test` run surfaced it. Guarded with a `typeof window === "undefined"` check that falls through to `VITE_API_URL`, since there's no tunnel host to derive anything from outside a real browser anyway.
- **Same-day follow-up, per client request: "No need to put this on the page" (quoting the per-criterion expand output above).** Misread as "drop per-criterion entirely" - `perCriterion`/`criteria` were removed from `ApplicantScoresOverview`/`applicantScoresOverview()` and `ReportSummaryPage.tsx`'s expand toggle and nested breakdown were deleted outright, back to a flat Category-only table.
- **Corrected same-day, per client clarification: "I just want to you to remove the Expand Criteria button only not the per-criteria breakdown."** The client wanted the per-criterion data always visible, not gated behind a click - not removed entirely. Restored `perCriterion`/`criteria` on `ApplicantScoresOverview`/`applicantScoresOverview()` (identical to the first version). `ReportSummaryPage.tsx` now renders the Category-level table as before, followed by an always-visible per-applicant card (no button, no expand/collapse state) showing every category's own criteria and their combined scores underneath - the interaction was the problem, not the data. Verified end-to-end again the same way as the original feature: seeded a temporary `PanelEvaluation` against the one real local application, confirmed the always-visible breakdown matched hand-calculation via the rendered page, then deleted the test data and restored the application's original status.
- **Final same-day pass, per client request: "Remove this also and leave only the table with scores"** (quoting the always-visible per-applicant breakdown card in full). Report Summary is now the Category-level table alone, nothing else - `perCriterion`/`criteria` removed from `ApplicantScoresOverview`/`applicantScoresOverview()` a second time, `ReportSummaryPage.tsx`'s per-applicant breakdown cards deleted outright. `GET /panel-evaluations/applicant-scores`'s response shape is back to exactly what it was before any of today's Report Summary iterations. This is the page's final shape, having gone table-only, table + expand button, table + always-visible breakdown, table-only again across four client-driven passes in one day; see `docs/architecture.md`'s Report Summary entry for the full iteration history.
- **Corrected same-day, per client clarification: "We're not on the same page. I just want you to remove the wordings only not the data in the table."** The previous pass over-corrected - the client's actual objection was to the *prose format* (per-row sentences like "Category: 20 of 25", "Criterion: 8 (0-10)"), not to having the per-criterion data at all. Restored `perCriterion`/`criteria` on `ApplicantScoresOverview`/`applicantScoresOverview()` a third time (identical computation to every prior version). `ReportSummaryPage.tsx` now renders one single table with one column per active Category *and* one column per active Criterion (plus Applicant/Job posting/Total/Panelists submitted) - the combined scores are all there, just as table cells under column headers instead of any sentence form. No expand/collapse, no cards, no prose section below the table. Verified end-to-end the same way as every prior pass: seeded a temporary `PanelEvaluation` against the one real local application, confirmed every category and criterion column matched hand-calculation via the rendered table, then deleted the test data and restored the application's original status.
- **Follow-up, same day: group the Report Summary table by Category and pin the Applicant column.** Client request: "the setup will be, in Row we have Name of applicant -> Job Posting -> Categories and below categories is the criterias... make use of CR1 for Criteria 1 and have a small tooltip to show the question or criteria... make the applicant name sticky column... Category and Criteria should group correctly." `ReportSummaryPage.tsx`'s flat category-then-criterion columns are now a real two-row `<thead>`: each Category header `colSpan`s over its own criterion columns plus a trailing "Score" column, and the criterion columns underneath are abbreviated `CR1`, `CR2`, ... **numbered fresh per category**, with the criterion's full name and raw max as a `title` tooltip instead of visible text. The Applicant column is `position: sticky; left: 0` (`.sticky-col` in `frontend/src/shared/styles/index.css`), opaque in both the default and row-hover states so scrolled columns don't show through underneath it, so the applicant stays identifiable no matter how far right the (still horizontally-scrollable via the existing `.table-wrap`) table is scrolled. No API or data-shape change - `criteria[].categoryId` already carried everything needed to group client-side. Verified the same way as every prior pass: seeded a temporary `PanelEvaluation`, confirmed the category groupings, per-category CR numbering, and sticky-column scroll behavior live in the browser, then deleted the test data and restored the application's original status.
- **Follow-up, same day: color the Report Summary table.** Client feedback: "put color not just plain. I currently doesnt like how it looks." Each category now carries its own accent (cycled through 5 tones pulled from the app's existing semantic palette - navy/green/blue/amber/red, `frontend/src/shared/styles/index.css`), applied to that category's group header and its "Score" sub-column only, so the individual criterion score cells stay neutral and easy to read while the grouping and each category's combined figure are colorized. The sticky Applicant column is now solid navy with white header text; `Total (avg)` gets a new gold tint (added `--color-accent-bg`/`--color-accent-border` tokens, matching the existing success/info/warning bg+border naming pattern) since it's the headline number; `Panelists submitted` renders as a small rounded badge. Added zebra striping and a brand-tinted row hover, scoped specifically to `.report-summary-table` so no other admin table's styling changes as a side effect. No data or API change - purely presentational. Verified live in the browser the same way as every prior pass: seeded a temporary `PanelEvaluation`, confirmed the colors/tints/sticky header render correctly and scroll correctly, then deleted the test data and restored the application's original status.
- **Follow-up, same day: full-width header divider, and "submitted/assigned" panelist counts.** Client feedback: "I see grey line but its not fully occupied below Total Avg and Panelist Submitted there's no grey line. Also in Panelist lets have 1/1 is only 1 and 1/5 if theres 5 etc." The divider gap was caused by `rowSpan={2}` on Applicant/Job posting/Total/Panelists submitted - a spanned cell has no boundary at the header's row1/row2 midline, so the line under the category headers had nothing to connect to past their edges. Fixed by dropping those `rowSpan`s and adding an explicit empty cell for each of those columns in the second header row instead, so every column has a real cell at that boundary and the table's normal border rendering draws one continuous line across the full width. Added `panelistsAssigned` to `ApplicantScoreRow`/`applicantScoresOverview()` (`GET /panel-evaluations/applicant-scores`, `backend/src/modules/panel-evaluations/panel-evaluations.service.ts`) - looked up per application's own job posting via one `panelAssignmentsRepository.findMany()` call across every posting, grouped into a count map, since this endpoint spans every posting rather than one fixed assigned-panelist count. `ReportSummaryPage.tsx`'s badge now shows `panelistsSubmitted/panelistsAssigned` (e.g. "1/1", "1/5"). Verified end-to-end the same way as every prior pass: seeded a temporary `PanelEvaluation`, confirmed the header divider now runs the full table width and the badge reads "1/1" against the one real local application's single assigned panelist, then deleted the test data and restored the application's original status.
- **Follow-up, same day: re-merge Applicant/Job posting's header cell.** Client request: "Merge Applicant name and the under empty cell same with job posting." The prior pass's full-width divider fix left a visible empty box under those two labels (their share of the new second header row, added so the divider line could run continuously). Applicant and Job posting go back to `rowSpan={2}` - one seamless tall cell, matching how they looked before that fix - while Total (avg) and Panelists submitted (not mentioned in this request) keep their explicit empty second-row cell so the divider still runs underneath them. Verified live in the browser: Applicant/Job posting now render as single merged cells with no blank box beneath, and the divider line under Total (avg)/Panelists submitted is unaffected.
- **Follow-up, same day: show every application status in Report Summary, add a Status column and filter.** Client request: "I have 1 hired applicant. I should also show it there. Lets add status column instead and a filter per status." The underlying cause: `findApplicationsWithScores()` (`backend/src/modules/panel-evaluations/panel-evaluations.repository.ts`) reused the same `TABULATION_STATUSES` filter (`FOR_INTERVIEW`/`QUALIFIED`/`NOT_QUALIFIED`) as the interview-stage tabulation worklist, so a scored application dropped out of Report Summary the moment it moved on to Compliance, Oath-Taking, HIRED, or a rejected/withdrawn status - exactly what happened to the client's one hired applicant. Dropped that status filter; `panelEvaluations: { some: {} }` (has this application actually been scored) is now the only inclusion condition, since Report Summary is a historical record, not a worklist. Added `status` to `ApplicantScoreRow`/`applicantScoresOverview()` and a Status column to `ReportSummaryPage.tsx` (same `badge`/`APPLICATION_STATUS_LABELS` styling `EvaluationRow.tsx` already uses elsewhere), plus a `Status` filter `<select>` (same `.filters-row`/`.field` markup `EvaluateApplicantsPage.tsx` already uses) that narrows the already-fetched rows client-side. Verified end-to-end: seeded a temporary `PanelEvaluation` against the client's real HIRED application *without* changing its status this time (confirming the fix directly, not by relying on a QUALIFIED workaround like every prior verification pass), confirmed it now appears with a "Hired" badge under "All statuses," confirmed the Status filter both hides it under an unrelated status and shows it again under "Hired," then deleted only the test `PanelEvaluation` (the application's real HIRED status was never touched).
- **Follow-up, same day: Publication filter, and drop the sticky Applicant column's blue styling.** Client request: "Lets also add filter per publication and remove the blue background color in Applicant name Make it the same setup with Status and Job posting." Added `jobPostingPublication` to `ApplicantScoreRow`/`applicantScoresOverview()` and a `Publication` `<select>` filter next to Status (built from whatever publications are actually present in the already-fetched rows, no separate API call), combining with the Status filter via AND. Removed the sticky Applicant column's navy background/white text (header) and blue bold text (body) overrides in `frontend/src/shared/styles/index.css` - it now falls back to the exact same plain header/cell styling as Job posting and Status, staying `position: sticky` while scrolling but with no color of its own. Verified live in the browser: Applicant now renders identically to Job posting/Status apart from staying pinned during horizontal scroll; the Publication filter (only "ROS-1"-style value present locally: "Publication March 2026") correctly keeps the one scored row visible when selected and hides it under an unrelated publication, matching the Status filter's already-verified behavior.
- **Follow-up, same day: narrow the Status filter's option list.** Client request: "Filter by status remove submitted, Under sifting, notqualified, Disqualified and Withdrawn since evaluation of applicant starts after Under sifting." `ReportSummaryPage.tsx`'s Status filter dropdown previously listed all 11 `ApplicationStatus` values; it now only offers the six that can meaningfully apply to a scored application, in pipeline order: Evaluation of Applicants, Qualified, Compliance to Requirements, Not Selected, Oath-Taking, Hired. Submitted/Under Sifting are excluded because an unscored application can never appear as a Report Summary row in the first place; Not Qualified/Disqualified/Withdrawn are excluded per the client's explicit request - a row in one of those statuses (if it ever occurs) still shows under "All statuses," it's just not individually selectable anymore. Verified live in the browser: the dropdown now reads exactly `["All statuses", "Evaluation of Applicants", "Qualified", "Compliance to Requirements", "Not Selected", "Oath-Taking", "Hired"]`.
- **Multi-posting applications + automatic panel score inheritance (Phase 1).** Client request: "Applicants can Apply Multiple Job posting as long as Applicant is not hired and the Panel points will automatically counted in all the Applicants applied jobs so that the applicant will not be evaluated in each job." Added `Application.scoreSourceApplicationId` (nullable self-relation, migration `20260821000001_add_application_score_inheritance`) - when set, this application's interview score is carried over from another of the same applicant's applications rather than scored here, since `PanelAssignment` is per-posting (a sibling posting's actual panel never scored them) but the interview rubric (`Category`/`Criterion`) is global (a score computed once is valid anywhere). Set at two points: a new application inherits immediately at creation if the applicant already has a scored/inheriting one (`ApplicationsRepository.createWithApplicationLetter()`); an existing sibling gets linked the moment a canonical application is actually scored (`PanelEvaluationsRepository.linkSiblingScoreSources()`, called from `PanelEvaluationsService.submit()`). Every reader of "this application's panel evaluations" (tabulation, the cross-posting applicant-scores/Report Summary source) resolves the pointer via one batched query, no N+1; the panel queue excludes inheriting applications outright, and `submit()` refuses to score one directly. Separately, `ApplicationsService.submit()` now 409s if the applicant already has any `HIRED` application anywhere - "as long as Applicant is not hired." Frontend: `JobPostingsListPage.tsx` gates the Apply button ("Already Hired") + a hint banner once hired anywhere; `MyApplicationsPage.tsx` shows "Interview score carried over from your application to \"X\"" when applicable. Verified end-to-end against the real local database (temporary second applicant + two temporary postings, deleted after) exercising the actual repository/service classes directly - 15/15 assertions passed covering creation-time inheritance, existing-sibling propagation, the scoring guard, queue exclusion (with an unlink/relink sanity check), and tabulation/applicant-scores resolution; the HIRED gate and "Already Hired" UI were also confirmed live in the browser against the one real HIRED applicant in this environment. The hierarchy/auto-hire-recommendation half of the client's request (highest-Salary-Grade recommendation, auto-close siblings once hired) is Phase 2, not yet built - see `docs/project-memory.md`.
- **Same-day follow-up: hierarchy hire recommendation + auto-close on hire (Phase 2).** Client request: "if applicant passed in all the Job Posting he applied he will be hired in the Higher position or Higher SG level." New pure `computeHireRecommendations()` (`frontend/src/features/admin/utils/hireRecommendation.ts`) flags the highest-Salary-Grade posting once an applicant has 2+ applications at `FOR_OATH_TAKING` - computed client-side in `EvaluateApplicantsPage.tsx` from data it already fetches, no new endpoint. `EvaluationRow.tsx` shows a recommendation hint on the recommended row, a caution on every other affected row (both in the Details modal and the Mark Hired confirmation itself), but the final hire decision stays admin-confirmed - per an explicit user-confirmed decision, the system never auto-flips a status on its own, consistent with every other stage in this pipeline. Once the admin does click Mark Hired, `ApplicationsService.markHired()` (extended) auto-closes the applicant's other still-open applications to `NOT_SELECTED` in one transaction, each with a system-generated remark, audit log entry, and regret email - reusing the existing status/audit-action/email machinery, no new `ApplicationStatus` value. `EvaluationRow` also now refetches the full applicant list after a hire (`onHired()`) so auto-closed siblings on other postings/pages update without a page reload. Verified end-to-end: a temporary-data script exercising `markHired()` directly (two open siblings at different Salary Grades plus an already-`WITHDRAWN` negative control) - 6/6 assertions passed; `computeHireRecommendations()` unit tests - 5/5 passed; and the full flow live in the browser (admin session via a minted JWT against temporary data) - clicking Mark Hired on the recommended SG-18 posting correctly flipped it to Hired and its SG-8 sibling to Not Selected in the same table, no reload. All temporary data deleted afterward.
- **Fix: `POST /api/categories` 500'd on a freshly-deployed database ("column `maxScore` does not exist").** Found live on the AWS EC2 (Singapore) instance while manually testing: `Add Category` failed with a generic `INTERNAL_ERROR` even though `prisma migrate status` reported the schema "up to date." Root cause, per the `20260819123048_rename_criteria_to_categories` migration's own comment: `criteria.maxScore`/`criteria.isActive` were originally added via a one-off script run directly against the one database this was authored on, never itself committed as a migration - so any genuinely fresh deploy (this AWS instance) never got those two columns at all, despite `_prisma_migrations` claiming everything was applied. `categories` itself was unaffected (confirmed via `DESCRIBE`, correctly has `isActive`/`weightPercent`) - only `criteria` was missing columns. New migration `20260821020000_add_criteria_max_score_is_active_columns` adds both, making it a verified no-op on databases (local dev) that already have them from the old undocumented script, and a real fix on databases that don't - a single migration works correctly everywhere instead of needing per-environment handling. No data at risk: both `categories` and `criteria` were empty on the affected AWS database (every `POST /api/categories` attempt failed inside the same transaction, so nothing partial was ever committed).
- **Follow-up, same day: the fix above's first version (`ADD COLUMN IF NOT EXISTS`) turned out not to be portable enough - AWS's MySQL rejected it as a syntax error (1064) when actually deployed.** `ADD COLUMN IF NOT EXISTS` requires MySQL 8.0.29+, and the AWS EC2 instance's server is older than that (local MariaDB accepted it, masking the incompatibility until the real deploy). The failed attempt also left the migration marked `failed` in AWS's `_prisma_migrations` (Prisma error P3018), blocking any further `migrate deploy` until resolved. Rewrote `20260821020000_add_criteria_max_score_is_active_columns/migration.sql` using the `INFORMATION_SCHEMA.COLUMNS` + `PREPARE`/`EXECUTE`/`DEALLOCATE PREPARE` pattern instead - works on any MySQL/MariaDB version, no floor to remember. Verified both branches directly against real MySQL (XAMPP's `mysql.exe`, not just Prisma's client, since testing via `$executeRawUnsafe` hits an unrelated Prisma-client-only limitation with nested `PREPARE`): the no-op branch against the real local `criteria` table (unchanged before/after), and the add branch against a disposable throwaway database's `criteria` table missing both columns (correctly added with the right types/defaults, `int NOT NULL DEFAULT 0` / `tinyint(1) NOT NULL DEFAULT 1`), both cleaned up afterward. AWS's failed migration record needs `npx prisma migrate resolve --rolled-back 20260821020000_add_criteria_max_score_is_active_columns` (safe - the syntax error meant zero DDL ever ran) before redeploying with the corrected file.
- **Fix: two multi-posting (score-inheritance) bugs found in manual testing - "duplicate interviews" and a confusing `MyApplicationsPage` view.**
  - **Duplicate interviews.** Phase 1's score inheritance only links two of an applicant's applications together at two moments: when a *new* application is created after a sibling is *already* scored, or the instant a sibling *gets* scored (`linkSiblingScoreSources()`). If an applicant applied to two postings *before either was scored*, neither had anything to link to yet - both could independently reach `FOR_INTERVIEW` and sit unlinked in two different panels' queues, so nothing stopped two panels from each scoring the same person separately. Closed with a self-healing repair pass added to `PanelEvaluationsRepository.findQueueForPanelUser()`: after the existing `scoreSourceApplicationId: null` filter, every remaining queue candidate is batch-checked (one query, grouped by applicant, not one query per candidate) against the applicant's other applications for one that's since been scored; any match is linked (same operation `linkSiblingScoreSources` does reactively) and excluded from the returned queue. The decision logic itself is a new pure, unit-tested `partitionQueueCandidatesToRepair()` export, same convention as `collectScoreSourceIds`/`mergeInheritedEvaluations`. A second, write-time guard closes the same race for two panelists racing to submit around the same time: new `PanelEvaluationsRepository.reconcileScoreSource()`, called from `PanelEvaluationsService.submit()` right before recording a score, freshly re-checks (not relying on the possibly-stale stored field) whether the applicant has since been scored elsewhere and links + refuses if so, instead of the stale `scoreSourceApplicationId !== null` check alone. Verified live against the real local database (temporary applicant + 4 temporary postings/panelists, deleted after): reproduced the exact race (two unlinked `FOR_INTERVIEW` siblings, one scored directly without going through the reactive link), confirmed the queue repair excludes the sibling and self-heals its `scoreSourceApplicationId`, and confirmed `reconcileScoreSource()` catches and links a second racing submission - 5/5 assertions passed.
  - **`MyApplicationsPage` showing stale/confusing status.** Reported as "I'm in Compliance to Requirements but I only see Evaluation of Applicants details, no upload." Root cause: score inheritance only ever shares the *interview score* between an applicant's sibling applications - each application's own pipeline `status` still progresses independently per posting (Compliance/Oath-Taking are still a per-posting admin decision), so an applicant with two open postings sharing one interview score can genuinely have one application at `FOR_COMPLIANCE` and the other still sitting at `FOR_INTERVIEW`. `MyApplicationsPage.tsx` was rendering each card correctly per its own `status` all along (confirmed live - the `FOR_COMPLIANCE` card already showed the checklist/upload UI exactly as expected); the actual gap was that the *canonical* (still-`FOR_INTERVIEW`) card gave no indication that a sibling application existed and had already moved further along, so it read like a stuck/broken status rather than "check your other application." Added the reverse hint: `Application.scoreInheritingApplications` (the self-relation's already-declared inverse side, no migration needed) is now included in `GET /api/applications/me` (`applicationWithPostingInclude`), and `MyApplicationsPage.tsx` shows "This interview score is also being used for your application to \"X\" (currently \<status\>)" on the canonical card, mirroring the existing "carried over from" hint already shown on the inheriting side. Verified live in the browser (minted-JWT session, temporary applicant with one `FOR_INTERVIEW` and one `FOR_COMPLIANCE` application sharing an inherited score, deleted after): both cards render as intended, the new hint correctly names the sibling posting and its live status.
- **Same-day follow-up: consolidate a multi-posting applicant into one row/card everywhere, and replace the automatic hire recommendation with a manual "Assign Position" action.** Client request: "I should only see one Application status in My Application page and for Admin I should only see one Applicant data, one to evaluate and one to score in Panel... in selecting the position the admin wants for the Applicant other job posting that the applicant applied will be free for others to apply... this is manual from Admin no need to assign the highest SG automatically." Kept the data model as-is (`Application` stays one row per applicant-posting pair, `scoreSourceApplicationId` unchanged) - this is entirely a view-layer consolidation plus one small backend addition, not a schema change:
  - **Admin Evaluate Applicants**: new `groupByApplicant()` (`frontend/src/shared/utils/groupByApplicant.ts`, generic, unit-tested) groups the already-fetched cross-posting application list by applicant before paginating - a single-posting applicant renders exactly as before, a multi-posting applicant collapses into one new `ApplicantGroupSummaryRow` (collapsed by default, click to expand into the unchanged per-posting `EvaluationRow`s) showing "Applied to N postings: Title A (Status), Title B (Status)...". When a specific posting is selected in the Job Title filter, every group is naturally a singleton (`@@unique([applicantId, jobPostingId])`), so the table reads identically to the old flat view with no special-casing needed - Sifting/PQE/Schedule-Interview/Compliance stay genuinely per-posting actions, unchanged.
  - **Manual "Assign Position", not automatic highest-SG.** The Phase 2 auto-recommendation (`computeHireRecommendations()`, picked the highest Salary Grade posting) is removed outright, replaced by a neutral `findOathTakingCandidates()` (`frontend/src/features/admin/utils/oathTakingCandidates.ts`, groups+filters only, no ranking - unit-tested including a regression guard that a lower-SG application is never dropped from the group). A new `AssignPositionModal` opens from the summary row's "Assign Position" button (shown only once 2+ of the applicant's applications reach `FOR_OATH_TAKING`), lists every qualifying posting with no default selection, and requires an explicit pick before Confirm enables. No new backend endpoint was needed - `ApplicationsService.markHired()` (from the earlier Phase 2 work) was already the exact "pick this one, auto-close every other open sibling to `NOT_SELECTED`" primitive; the modal just calls it with whichever application id the admin actually chose, instead of that choice being implicit in which row the admin happened to be looking at. The individual per-posting `EvaluationRow`'s own Mark Hired button is hidden (with an explanatory note) for any application caught in a 2+-way group, so the consolidated picker is the only path once there's a real choice to make.
  - **`MyApplicationsPage`**: a single applicant still only has one identity, so this needed no new grouping key - `applications.length <= 1` renders exactly as before; 2+ applications now render as one outer card ("You've applied to N job postings") with each posting as a `.posting-subsection` (new CSS, a left-accent-bordered block, avoiding double-boxed nesting with the existing `.card-inset` detail blocks reused unchanged inside it) instead of N separate top-level cards. The two score-inheritance hint lines added earlier today (`scoreSourceApplication`/`scoreInheritingApplications`) became redundant once everything is visibly in one place and were replaced with a single "Your interview score is shared across all the applications below" line. Supersedes today's earlier "reverse-relation hint, not status propagation" decision - see `docs/decisions.md`.
  - **Report Summary / Applicant Scores modal**: `ApplicantScoreRow` gained `applicantId` (`panelEvaluationsRepository`'s `applicantId` was already on the row, just not projected onto the response type - no query change) so a reliable grouping key exists (previously only `applicationId` + a formatted `applicantName` string). New `combineApplicantRows()` (`frontend/src/features/admin/utils/combineApplicantRows.ts`, unit-tested) collapses a multi-posting applicant's rows into one - since `resolveInherited()` already makes their score data identical, there's nothing to expand, just a combined "Postings applied" cell (each with its own status badge in Report Summary; joined into one string for the narrower Applicant Scores modal) alongside the one shared score. `ApplicantScoresModal`'s `rankRows()` now ranks the already-deduped array.
  - **Panel My Interviews**: `findQueueForPanelUser()` gained a new batched `attachOtherApplications()` pass (same one-query-not-N convention as the existing repair pass) exposing `otherApplications: { jobPostingTitle }[]` per queue entry - purely informational, not a dedup change, since the queue can (rarely, correctly) show two genuinely-distinct not-yet-scored applications for the same person if the same panelist is assigned to both of their postings' boards before either is scored. `InterviewRow.tsx` shows "Also applied to: X, Y" when non-empty.
  - Verified end-to-end live in the browser against real seeded data (two applicants: one with 2 applications both reaching `FOR_OATH_TAKING` at different Salary Grades, one with 2 unscored `FOR_INTERVIEW` applications sharing a panelist, both deleted after): the admin table showed exactly one collapsed row per applicant with an "Assign Position" button appearing only on the oath-taking one; picking the **lower**-SG posting in the modal (deliberately, to prove there's no hidden highest-SG bias) correctly marked it `HIRED` and auto-closed the higher-SG sibling to `NOT_SELECTED`; `MyApplicationsPage` immediately reflected both outcomes in one consolidated card; Report Summary and the Applicant Scores modal both showed the applicant as a single ranked row listing both postings; the panel queue showed the "Also applied to" hint on both of the second applicant's still-pending rows. Full `npm run typecheck`/`lint`/`test` passed on both workspaces throughout.
- **Same-day follow-up: the panel's My Interviews queue itself still showed one row per posting for a multi-posting applicant, not one.** Client feedback after using the feature above: "In scoring of applicant with multiple application I can still see multiple scoring. How about 1 only even if the applicant has multiple application just indicate in Job posting what are the other application will this score will go." This was the one screen the earlier consolidation pass had deliberately left ungrouped (each queue row still looked like a distinct interview task) - reconsidered given the client wants exactly one scoring form regardless. `MyInterviewsPage.tsx` now dedupes the fetched queue with the same `groupByApplicant()` used elsewhere, rendering only the earliest-submitted of an applicant's queue entries (matching the backend's own canonical-selection convention) - a single-posting applicant is unaffected. `otherApplications` (added earlier for an informational hint) was tightened same-day from "every other application, any status" to "every other still-open, not-yet-scored, unlinked application" - exactly the set `PanelEvaluationsRepository.linkSiblingScoreSources()` will actually point at this one once it's scored - so the row's "This score will also count for: X, Y" line is never misleading about a sibling that's already scored independently or terminal. No new endpoint: submitting the score against the one rendered row still triggers the existing propagation to every linkable sibling automatically. Verified live end-to-end against real seeded data (one applicant, two unscored applications on two postings both assigned to the same panelist, deleted after): the queue correctly showed exactly one row with the hint naming the sibling posting; submitting a score there confirmed via direct DB query that the canonical application received the real `PanelEvaluation` and the sibling's `scoreSourceApplicationId` was correctly set to point at it.
- **Same-day follow-up: Evaluate Applicants' pipeline actions themselves now move a multi-posting applicant's applications together, not just the view.** Client feedback: "Lets make this also with Evaluate applicants in Admin... 1 data row only, one button for View documents, Sift, PQE score, etc. All application will move at the same time." Earlier the same day the admin table already collapsed a multi-posting applicant into one summary row, but acting on them still meant expanding it back into N separate `EvaluationRow`s and repeating every action once per posting. `EvaluationRow.tsx` is now generalized to take `applications: AdminApplication[]` instead of a single application (length 1 for a single-posting applicant, unchanged behavior; 2+ for a multi-posting one) - every action (Sift, PQE score entry, Schedule Interview, Move to Compliance, Not Selected, Disqualify, Schedule Oath-Taking) now runs once per group member via a new `applyToGroup()` helper (`Promise.allSettled`, not `Promise.all`, so one posting's failure doesn't silently swallow the others that succeeded), filled in from one form/click. `ApplicantGroupSummaryRow` (the old collapse-and-expand component) is repurposed as a fallback for the one real edge case this can't paper over: a group whose applications have *already* diverged in status (only possible from data older than this feature, or a genuine anomaly) - `EvaluateApplicantsPage.tsx` only reaches for it when `group.rows.some(app => app.status !== group.rows[0].status)`, so a real applicant with out-of-sync data (found live in this environment's own data, not a seeded test) still gets a working per-posting view instead of silently losing its action buttons. Mark Hired stays the one action that does *not* move together (hiring is inherently exclusive to one posting) - a group at Oath-Taking still gets the existing "Assign Position" picker instead. The Sift decision now shows every posting's own qualification-standards match panel in the one form (client explicitly accepted that a single Qualified/Not Qualified call applies uniformly even though QS can differ by posting - see `docs/decisions.md`). No backend changes - every existing single-application endpoint is just called once per group member from the frontend. Compliance-item-level verify/reject (inside "Manage Compliance") is *not* yet mirrored across the group - noted as a known follow-up, not silently overlooked. Verified live end-to-end against real seeded data (one applicant, two `UNDER_SIFTING` applications on two postings with deliberately different qualification standards - one requiring only a Bachelor's, one requiring a Doctorate - deleted after): the one Sift form showed both postings' own MEETS/BELOW badges correctly before a single "Qualified" decision moved both applications to `QUALIFIED` together (confirmed via direct DB query); continuing the same pair through PQE score entry and Schedule Interview confirmed both stayed a single action producing two updated applications each time. Also confirmed live against real data that an already-out-of-sync applicant correctly falls back to the per-posting expansion instead of breaking.
- **Same-day follow-up: Interview Panel and Group pages also grouped by applicant, fixing redundant repeated names.** Client feedback: "In Group page and Interview Panel page I also see multiple redundant names. Lets also make this in one." Both pages' applicant tables were sourced from the same flat `listApplicationsForAdmin()` list already grouped elsewhere today, so a multi-posting applicant appeared once per posting on both. Fixed with the same `groupByApplicant()` utility used on Evaluate Applicants, but with two different resolutions since the two pages' underlying actions have different scopes:
  - **Interview Panel** (`PanelAssignmentsPage.tsx`): a panel assignment is genuinely per-posting (different postings can reasonably want different boards), so this stayed a *display-only* grouping - one row per applicant, listing every posting they applied to with its own "Assign Panel" button and its own assigned-panelist list. The existing bulk-select-and-assign flow needed no semantic change at all (it already correctly resolved "N applications selected" down to "the distinct postings behind them" before adding panelists) - checking one applicant's row now just selects every one of their own application ids at once, which is exactly what feeds correctly into that existing flow. Only the *display* count text was wrong (a multi-posting applicant's row previously would have inflated "N applicants selected" by counting applications, not people) - fixed with a new `selectedApplicantCount` (distinct `applicant.id`s among the selection) used everywhere that number is shown.
  - **Group** (`GroupsPage.tsx`): a Group Dynamics Evaluation group is about *people*, not postings, so this needed an actual selection-semantics change, not just a display fix - a multi-posting applicant should only ever produce one `ApplicantGroupMember` row (the schema's `applicationId` foreign key is just how a member is stored, not a deliberate multi-posting distinction). `selectedApplicationIds: Set<string>` became `selectedApplicantIds: Set<string>`; a new `representativeApplicationIdByApplicant` map (built from the full, unfiltered applicant list, always resolving to the same first-seen application per person) is used only at the moment of actually calling `createApplicantGroup`/`updateApplicantGroup`, so the UI's selection stays person-shaped throughout while the API still gets exactly one application id per selected person. Existing group membership already stored under any one of an applicant's application ids (from before this fix, or from `startEditMembers` resolving a stored membership back to "who is this") is looked up via a new `applicantIdByApplicationId` map, so the "Group(s)" column and pre-selected checkboxes when editing an existing group's members both stay correct regardless of which of a person's applications the group happens to reference.
  - Verified live end-to-end against the real multi-posting dummy data already in this environment (Francisco Lopez: 3 applications; Elena Cruz, Roberto Diaz: 2 each): Interview Panel showed exactly one row per applicant with correct per-posting "Assign:" buttons and assigned-panelist lists, and selecting Elena Cruz's one row correctly opened the bulk-assign modal scoped to "1 applicant... covers 2 job postings." Group showed exactly one row per applicant; selecting Francisco Lopez (3 applications) and Miguel Aquino (1) together and creating a group correctly produced exactly 2 `ApplicantGroupMember` rows (confirmed via direct database query, not 4), and re-opening that group's "Members" editor correctly re-checked both of their rows. Cleaned up the verification group afterward.
- **Same-day follow-up: Interview Panel could still assign a *different* panel to each of a multi-posting applicant's postings.** Client feedback: "In interview panel, there should be no function adding different Panel in each Job application for 1 applicant. Same panel in all Positions applied." The per-posting "Assign: `<title>`" buttons added in the grouping pass above were the direct culprit - each opened its own modal with an independent panelist selection, so nothing stopped an admin picking different panelists for the same person's different postings even though only one of their applications ever actually gets interviewed/scored (score inheritance). Replaced with a single "Assign Panel" button per applicant row, regardless of how many postings they applied to: `PanelAssignmentsPage.tsx`'s `assigningApplication: AdminApplication | null` became `assigningGroup: AdminApplication[] | null`, pre-filled with the *union* of whoever's currently assigned across any of the applicant's postings (so an admin picking up an already-divergent assignment, e.g. from before this fix, sees everyone rather than silently dropping someone), and `handleSaveAssignment()` now applies the exact same selected panelist list to every one of the applicant's postings in one call (add/remove diffed per posting against its own current roster, same underlying `createPanelAssignment`/`deletePanelAssignment` calls as before, just looped). The multi-select bulk "Assign Panel to Selected" flow needed no change - it already applies one shared panelist list across every selected posting in a single `bulkCreatePanelAssignments()` call, so it was never able to set different panels per posting to begin with. Verified live: opening "Assign Panel" for Elena Cruz (2 postings) showed one modal listing both postings with a note that they share one panel; unchecking one panelist and saving updated *both* of her postings' assigned panels identically (confirmed via the table); restored to the original two-panelist assignment afterward.
